# 一键模型部署与自治集群远期计划

> 状态：现行（MODEL-FLEET 与主节点本地认证/集群治理计划；真实环境门另行验收）
>
> 更新日期：2026-08-19
> 适用范围：MODEL-FLEET 专项——模型来源/凭据、内容寻址工件库、运行时准入、本地认证与 Tailscale 绑定、存储退场与部署档案；优先级以《总体下一步计划》L4-MODEL-FLEET 为准
>
> 当前状态以本页 `2026-08-19 CLUSTER-JOIN-S0`、`2026-08-10 当前复核`、`MF-AUTH-N2L 本机状态适配复核`、`MF-AUTH-N2 客户端接线复核`、`MF-AUTH-N1 实施复核` 及总计划入口为准；更早的无日期增量描述仅保留历史证据。

> **MF-AUTH-N1W Web 首登接线（2026-08-10）**：前端已增加主节点认证门，覆盖 owner 首次创建、Auth App 二维码/字符串本地生成、首次 TOTP 确认、恢复码一次展示、TOTP/恢复码登录、会话恢复和 logout；二维码由本地 `qrcode` 生成，不调用第三方 HTTP。Bearer 仅保存在 `sessionStorage`，目前只随认证/用户请求发送。gateway 新增 `/api/auth/capability`，`auth/users` 固定发往 loopback control-svc，不因迁移开关回退到无认证的 legacy；旧 Python 单体明确 404 时保留原入口，其他能力探测错误 fail-closed。前端单元 `29/29`、浏览器 `4/4`、gateway `107/107`，两端构建通过，前端生产依赖审计 `0 vulnerabilities`。本票是 `Completed（开发门）`，不把旧单体兼容入口或 mock 浏览器门表述为生产认证完成。

> **MF-AUTH-N1A 业务授权与账户管理接线（2026-08-10）**：gateway 全局守卫按公开认证、节点机器信任、已登录业务、owner/admin 管理分流；Bearer 通过 control `/auth/session` 校验，`QLH_AUTH_REQUIRED` 支持显式 rollout，节点注册/心跳/发现/模型文件分发保留机器信任路径。control 新增会话列表/撤销，用户列表返回 TOTP 状态与活跃会话数且不含 token hash，admin 不得替换 owner Auth App。Web 账户工作区支持会话撤销、恢复码轮换、用户创建/编辑/暂停/启用、Auth App 二维码/字符串配置和恢复码一次展示；移动端账户页收成全宽工作区。control `313/313`、gateway `113/113`、前端 `29/29`、浏览器 `5/5`，构建通过、前端生产依赖审计 `0 vulnerabilities`。N1A 为 `Completed（开发门）`，N1 整体仍为 `Validating`。

> **MF-AUTH-N2 实施复核（2026-08-10）**：本地主节点已接入 Tailscale 绑定 API：本人/owner/admin 受会话保护的列表与发起、pending→active 确认、旧 active→revoked 原子换网、撤销幂等、跨用户身份冲突拒绝和 `credential_ref` 脱敏；OAuth Apps 明确保持未启用。N2 契约 `3/3`、control 全量 `309/309`、构建通过。真实 Tailscale CLI/浏览器授权、旧节点网络侧实际撤销、完整客户端 UI 和双机实网仍是 `Validating` 外部验收门。

> **MF-AUTH-N2 客户端接线复核（2026-08-10）**：Web 账户工作区已接当前绑定列表、官方 CLI/本机状态授权方式选择、pending 确认、稳定 `tailnet_id + tailscale_user_id + node_id` 录入、换网历史和撤销；本地 API client 只提交稳定身份字段，不接收密码、token 或 `credential_ref`。前端单元 `30/30`、浏览器 `5/5`、构建通过。客户端开发门完成，N2 整体保持 `Validating`，真实 Tailscale CLI/浏览器、旧节点网络撤销和双机实网集中到最终联合验收。

> **MF-AUTH-N2L 本机状态适配复核（2026-08-10）**：control-svc 新增受认证保护的 `GET /auth/tailscale/local-status`，以无 shell 子进程执行 `tailscale status --json`；支持跨平台 CLI 查找、`QLH_TAILSCALE_CLI` 覆盖、有界超时/输出、5 秒缓存和并发合并。解析器仅投影 `tailnet_id` 候选、`tailscale_user_id`、`node_id`、主机/DNS 展示与受信 Tailscale IPv4/IPv6，禁止返回 loginName、公钥、Peer/Health、stderr、CLI 路径或 token。Web 可读取并自动填充，但必须由用户继续发起 pending 和确认；MagicDNS 后缀只作候选，不静默确认为永久 ID。N2 定向契约 `7/7`，Windows 本机只读探测识别身份和双栈地址且无禁返字段；未执行真实绑定、换网或旧节点撤销，N2 仍为 `Validating`。

> **MF-AUTH-N1 Linux adapter 续票（2026-08-10）**：Linux Secret Service credential adapter 已实现并接入默认凭据存储；`secret-tool` 通过标准输入/输出完成存取，SQLite/JSON 只保存随机 opaque handle，覆盖/删除清理 keyring 项，服务不可用或命令超时 fail-closed。适配器契约 `2/2`、control 全量 `311/311`、构建通过。Web 接线已由后续 N1W 完成，Linux 实机 DBus/Secret Service 与完整安装首登仍待最终联合发布门，N1 保持 `Validating`。

> **MODEL-TOOLS P0 实施复核（2026-08-10）**：模型资产只读工具三件套已落地到 `scripts/model_tools/`：`gguf_inspect`、`gguf_verify`、`models_sweep`，统一入口为 `python scripts/model_tools.py inspect|verify|sweep`，支持人类输出与 `--json`。定向测试 `5/5`；真实 `H:\qlh_models` junction 根目录 sweep 为 GGUF×2、SD manifest×5、PyTorch 目录×2 全部有效，`orphan_candidates=0`。该阶段 Python 全量为 `1714 passed / 27 skipped`。P0 仍为 `Validating`，剩余安装包/跨平台入口接线和发布路径复核；P1 后续状态由下方磁盘治理复核覆盖。

> **MODEL-TOOLS P1 磁盘治理实施复核（2026-08-10）**：统一入口新增 junction 感知的 `model_disk_usage` 与 `models_clean`。统计输出顶层资产 logical/allocated/unique allocated bytes；清理默认 dry-run，只有 `--apply --confirm CLEAN` 才进入删除，并在执行前拒绝根目录本身、越界路径及 junction/reparse point。缓存/旧备份需额外 opt-in，重复模型永远只列 review。定向测试 `11 passed / 1 skipped`、Python 全量 `1720 passed / 28 skipped`；真实 `models` junction / `H:\qlh_models` 只读统计为 204 文件、41,238,213,220 bytes，dry-run 的 8 个 cache 默认安全删除数 0，本票未删除资产。磁盘治理开发门完成；P1 下一票为 SD prompt/sampler CUDA 侧车批测。

> **MODEL-TOOLS P1 SD 批测实施复核（2026-08-10）**：新增 `sd15_prompt_batch` 与 `sd15_sampler_matrix`，固定输出上限 64，PNG/contact sheet/JSON 原子报告，资产校验失败 fail-closed；不写模型目录或主节点 SQLite。`SD15GenerationRequest`/service/worker 协议允许 EulerDiscreteScheduler、DDIMScheduler，且请求结束恢复原 scheduler。CUDA 侧车真实 smoke 使用原版 SD 1.5：4-step Euler+DDIM 2 输出、2 prompts×1 seed 2 输出，均自动图像门通过、hash 唯一、CLI `exit_code=0`。相关定向 `91 passed / 6 skipped`、CUDA 侧车恢复 `2/2`，Python 全量 `1723 passed / 30 skipped`。P1 SD 批测开发门完成；完整 10-seed矩阵、人工目视和安装包/跨平台入口仍是 `Validating` 外部门，下一票转 P2。

> **MODEL-TOOLS P2 同步检查实施复核（2026-08-10）**：新增 `models_sync_status inventory|compare`。schema v1 清单不携带根目录、盘符或绝对路径；默认结构摘要用于快速巡检，显式 `--full-hash` 才读取内容，两种模式拒绝混比。比较输出缺少/多出/不一致，CLI 以 `0/1/2` 区分一致/差异/无效输入，外来清单 fail-closed，嵌套 junction/reparse point 不跟随。定向 `24 passed / 2 skipped`、Python 全量 `1728 passed / 31 skipped`；真实 `models` 与 `H:\qlh_models` 均识别 10 个顶层资产且结构比较一致，输出无绝对路径。本票未进行 41.24 GB 全内容重哈希，也不替代真实双机/跨平台门；下一票 `llm_smoke_matrix`。

> **MODEL-TOOLS P2 LLM 冒烟矩阵实施复核（2026-08-10）**：新增 `llm_smoke_matrix`，固定中文/英文/精确 JSON/中等上下文 marker 四项；注册表中每个模型格式独立离线 worker，HF 缓存落临时目录并清理，token、单元数和超时有硬上限。缺资产、CUDA 或 RAM/VRAM 余量不足均先跳过，单元失败不阻断矩阵；报告不保存生成原文或模型绝对路径，并分别记录执行门和覆盖门。Qwen 1.8B GGUF/Safetensors `2/2 units、8/8 jobs` 真实通过；完整注册表发现 10 单元，2 通过、6 缺资产、DeepSeek 7B 两格式分别因 RAM/VRAM 余量跳过，0 失败，因此执行门通过但覆盖不完整。定向 `29 passed / 2 skipped`、Python 全量 `1738 passed / 31 skipped`；结果见下条 GGUF 预检复核。

> **MODEL-TOOLS P2 GGUF 转换预检实施复核（2026-08-10）**：统一入口新增 `gguf_convert`/`gguf-convert`，预检阶段仅做注册表或显式源目录的 config/tokenizer/分片 index、架构注册、Python 依赖、转换器/`llama-quantize`、目标冲突、目标卷空间和路径边界检查；当时无 `--apply`，不创建 staging、不调用转换器、不覆盖现有工件。F16/BF16/F32/Q8_0 规划为单步转换，Q4/Q5/K 系列规划为 F16→量化器两步，并输出无绝对路径的占位符命令。Qwen 1.8B F16 真实预检通过（两片权重 `3,673,678,272` bytes、index 完整、当前盘余量约 `80,245,624,832` bytes）；Q4_K_M 因缺 `llama-quantize` 阻塞，两个目标均未生成。定向 `40 passed / 2 skipped`、Python 全量 `1744 passed / 31 skipped`；执行事务已由下条完成。

> **MODEL-TOOLS P2 GGUF 转换执行事务实施复核（2026-08-10）**：默认 dry-run 保持只读，真实写入要求 `--target --apply --confirm CONVERT` 精确确认。执行在目标同卷 staging 中完成，无 shell、强制离线且有界超时；Q4/Q5 量化前验证 F16 中间件，最终输出通过 GGUF 结构与全 SHA256 校验后，按“摘要旁车先、GGUF 后”以无覆盖硬链接发布。失败、超时、取消和目标/旁车竞态均不覆盖并清理临时状态。合成真实子进程覆盖 F16/Q4 成功与故障边界，定向 `48 passed / 2 skipped`、Python 全量 `1752 passed / 31 skipped`。真实 Qwen 只做预检和未确认执行拒绝，没有转换或新工件。量化器固定/打包开发票已由下条完成；真实转换仍需用户另行批准，Linux 真机门待设备到位。

> **MODEL-TOOLS P2 llama-quantize 固定与分发实施复核（2026-08-10）**：机器锁固定 llama.cpp `47e1de77aa0f06bf73cfd8c5281d95979f89fcbe`；无网络构建脚本统一 CPU-only、静态库、关闭 CUDA/native/OpenMP/UI/HTTPS，Windows GNU 额外静态链接 GCC/C++ 运行库并验证 PE 仅依赖系统 DLL。Windows 受管 EXE/manifest/确定性 ZIP 已实际产出，EXE SHA256 `d91893de…30794`，ZIP SHA256 `e3361e96…0b32`；`gguf_convert` Q4 dry-run 已报告 `managed_package/verified`。PyInstaller 与 Linux `.deb` 已接入固定安装路径；Linux 真机 build/ldd/安装执行仍待验收，下一票 Linux 工具链门，真实模型转换继续要求用户批准。

> **状态（2026-08-10）**：无硬件控制面 `M0.1-M5.0`、存储主线 `M1.2/M1.3`、显存 `MF-MEM-N0-N2P` 以及认证 `MF-AUTH-N1W/N1A/N2L/N2` 客户端开发门均已完成；`MF-AUTH-N1/N2`、`NET-N1`、完整安装和跨节点实网验收继续保持 `Validating`。认证、会话、绑定和恢复资产只存用户主节点 SQLite/OS credential store，不引入开发组远端依赖。当前执行维持 N2 真实 Tailscale/跨平台联合验收；UVM `MF-MEM-N3` 仅在原生 Linux + 32 GB RAM 条件满足时评估。
>
> **当前复核（2026-08-10）**：`M1.3` 已完成，远端 PostgreSQL 已从生产运行图和安装包退场；`MF-AUTH-N1` 与 `MF-AUTH-N2` 均保持 `Validating`，其中 `MF-AUTH-N1W`、`MF-AUTH-N1A`、`MF-AUTH-N2L` 和 N2 客户端开发门已关闭。当前相关基线为 Python `1714 passed / 27 skipped`、control `317/317`、gateway `113/113`、前端单元 `30/30`、浏览器 `5/5`；N2 定向契约 `7/7`。当前执行维持 N2 真实 Tailscale/跨平台联合验收；真实 Linux、完整安装和双机实网集中到最终验收。

> 基线刷新注记（2026-08-13）：上述 2026-08-10 复核基线保留为当时证据；当前全量为 Python `2085 passed / 8 skipped`、control `313/313`、gateway `114/114`、前端单元 `38/38`、浏览器 `5/5`（见[测试与评判标准 §4.1](测试与评判标准.md)）。

> **MF-AUTH-N1 实施复核（2026-08-10）**：本地主节点认证已进入 `Validating`。control-svc 已提供 owner bootstrap、TOTP 字符串/`otpauth://` QR 载荷、首次确认、恢复码、Bearer 本地会话、失败限速/锁定、logout、用户 CRUD、会话列表/撤销和审计；SQLite schema v7 新增会话哈希与登录锁定表，TOTP 原文只经 OS credential store 保存。Windows DPAPI + Linux Secret Service adapter 已接入，适配器契约 `2/2`、认证契约与会话/角色契约均通过；Web 首登/登录/logout 已由 N1W 接线，N1A 已接入 gateway 全局授权矩阵和账户工作区。control `313/313`、gateway `113/113`、前端 `29/29`、浏览器 `5/5` 通过；N1 仍不转 Completed：Linux 实机 DBus/Secret Service、完整安装首登、默认发布拓扑切换和真实跨节点撤销仍待最终联合发布门，暂不接 Tailscale OAuth。
> **计划代号**：`L4-MODEL-FLEET`
>
> **调研日期**：2026-08-05
>
> **更新日期**：2026-08-10

> **M1.2 实施状态**：`Completed（存储实现与双平台隔离环境门，2026-08-09）`。第七片新增可重复环境探针：Windows 11 上的干净目录、损坏库、NTFS 写入 deny ACL，以及 WSL2 Ubuntu 22.04 ext4 上的干净目录、损坏库、目录 `0555`/文件 `0444` 均验证 Python/Node 共库、schema v5 和 fail-closed；所有探针只使用临时目录并已清理。Python `1720/1720`（另 7 skipped）、control-svc `297/297`、gateway `104/104`；本票另有 Python 定向 `116/116`。完整安装与首次认证不属于本阶段，认证登录仍未开放。
>
> **M1.3 实施状态**：`Completed（远端 PostgreSQL 退场，2026-08-10）`。control SQLite 迁移至 schema v6 `storage_retirement` 状态；`storage:retire-postgres prepare|retire|verify` 在清理前完成加密备份、恢复演练、脱敏 manifest、哈希一致性校验和环境清理。control-svc 生产图移除 ConfigDao/Outbox/PostgresProjector，Python API/scheduler/inference-svc 的会话、设置、模型注册、审核票据、邮件配置和推理历史均落主节点 SQLite；安装包 requirements/spec/launcher 不再携带或预加载 psycopg2。Python `1709 passed / 27 skipped`、control-svc `300/300`、gateway `104/104`。旧 `src/db.py` 与历史 projector 仅保留迁移审计，不再是运行时依赖；下一票 `MF-AUTH-N1`。
>
> **NET-N1 实施状态**：`Validating（本地双栈实现完成，2026-08-09）`。TCP、Python API、control-svc、gateway、分发服务和 PC/TUI/Android IPv6 authority 已统一；Windows 真实三服务与 Windows/WSL socket 门通过。当前基线为 Python `1729 passed / 7 skipped`、control-svc `301/301`、gateway `104/104`，Android Full Debug Kotlin 编译通过。双机纯 IPv6/混合网、真实 Tailscale IPv6/MagicDNS 和安装包仍待验收。
>
> **适用范围**：模型目录、模型获取与导入、工件校验、节点部署、Tailscale 集群配置、主节点 SQLite 持久化、用户认证/管理和远端 PostgreSQL 退场/兼容边界
>
> **关联计划**：[总体下一步计划](总体下一步计划.md)、[微服务架构改造计划](archive/微服务架构改造计划.md)、[从节点部署配置指南](从节点部署配置指南.md)、[混合分布式推理体系规划](archive/混合分布式推理体系规划.md)

---

## 1. 目标与结论

本计划面向远期的“下载即部署”体验：用户不再手工寻找文件名、拼接目录、复制 `.env` 或逐台设备同步模型。用户可以从受控目录选择模型，也可以填写 Hugging Face 仓库、GGUF 文件或本地路径；系统完成下载、检查、注册、设备匹配、分发、试加载、启用和回滚。

目标体验对标 Ollama，但不是简单包一层 Ollama：

1. 提供类似 `pull / create(import) / list / show / delete` 的稳定操作语义和可观察下载进度。
2. 支持 Hugging Face Safetensors/Transformers 仓库和 GGUF 文件，允许后续增加 ModelScope、Diffusers 与私有制品库适配器。
3. 为 QLH 的 PyTorch、llama.cpp、完整 Worker、任务链和后续图像 Worker 生成明确的部署计划。
4. 用户可以登记自己的 Tailscale 地址和主节点，设备首次接入不需要远端数据库密码。
5. **主节点 SQLite 是最终事实源**：干净安装、升级、模型管理、节点接入、本地推理和认证都能在没有远端数据库时完整运行。
6. 远端 PostgreSQL 只保留用户主动开启的兼容导出/迁移能力，不能成为 QLH 的运行前提；未来可以删除该依赖而不迁移用户资产到开发组。
7. 相同模型名称不能代替相同工件身份。参与同一次 PyTorch 层流水线或 GGUF 执行池的节点必须使用完全一致的内容摘要。

核心架构结论：

- 模型权重使用**内容寻址工件库**，数据库只保存元数据、引用、任务和部署状态，不保存大文件。
- 主节点采用**本地 SQLite 事务库**作为唯一控制面事实源；远端 PostgreSQL 降级为默认关闭的兼容导出/迁移层，既不是投影事实源，也不是备份前提。
- 下载、适配、部署和激活分为不同状态。下载成功不等于模型可加载，单机可加载也不等于可参加分布式推理。
- Tailscale 负责设备准入后的加密网络和稳定寻址；QLH 仍保留集群 ID、共享集群密钥、角色和模型摘要校验。
- QLH 登录采用主节点本地 Auth App/TOTP，不绑定 email；Tailscale 只用于 tailnet/用户/节点绑定和网络授权，不把 Tailscale 账号误写成 QLH 的通用身份提供方。
- 用户、TOTP 密钥、恢复码、Tailscale 绑定和认证审计均由用户自己的主节点管理，默认不进入开发组服务、公共账号目录或远端 PostgreSQL 投影。
- 用户自己的模型工件、adapter、配置、集群档案、任务记录、会话、认证数据和备份都属于用户自持资产；QLH 开发组不保留远端副本、不提供远程 reset，不影响上游模型许可证和第三方内容权利。

---

## 2. 为什么不能直接复制 Ollama

Ollama 已经验证了以下用户体验是合理的：

| Ollama 能力模式 | QLH 应吸收的设计 | QLH 额外要求 |
|---|---|---|
| pull 流式返回清单、分片、校验和完成状态 | 下载任务必须可查询、可取消、可恢复 | 下载后还需按节点能力生成部署计划 |
| create/import 接受 Safetensors、GGUF、adapter 与参数 | 本地导入和模型配方应成为一等对象 | 必须区分 PyTorch、GGUF、Diffusers 和分布式执行能力 |
| list/show/delete | 提供稳定的模型生命周期 API | show 必须包含来源 revision、摘要、部署节点和真实健康状态 |
| SHA256 blob 与 manifest | 相同内容只存一份，标签只引用摘要 | 分发到 Worker 后仍要逐文件和聚合摘要校验 |
| 模型名与 tag | 给用户保留易读别名和版本 | 调度不能只按别名，必须锁定不可变 artifact digest |

QLH 不在第一阶段追求与 Ollama API 字节级兼容。优先实现一致的用户体验和内部正确性；兼容 `/api/pull`、`/api/show`、`/api/tags` 等接口可以作为后续适配层，不能反向污染 QLH 的分布式部署契约。

---

## 3. 当前基础与真实缺口

### 3.1 可以复用的基础

| 现有模块 | 可复用能力 |
|---|---|
| `src/model_config.py` | 内置模型元数据、Hugging Face ID、模型类型和显存建议 |
| `src/model_sync.py` | 主节点模型 manifest、Range 断点续传、逐文件 SHA256、聚合摘要和目录镜像 |
| `src/model_downloader.py` | Hugging Face/ModelScope 下载引导与模型文件存在性检查 |
| `src/model_module.py`、`src/llama_engine.py` | PyTorch/Safetensors 与 llama.cpp/GGUF 的真实加载入口 |
| `src/bootstrap.py`、`src/node_config.py` | Tailscale 候选发现、首次接入、主节点地址和集群密钥持久化 |
| `src/task_journal.py` | 主节点本地 SQLite WAL、事件 ID、恢复和幂等边界的已有实践 |
| `control/` 模型注册域 | 用户模型 CRUD 与本地 JSON 原子写基础 |
| Android SAF/Room | Android 本地模型目录选择与本地持久化基础 |

### 3.2 不能误写为已完成的能力

- 当前下载器主要围绕 Qwen-1.8B 和固定文件名，不是通用模型中心。
- 当前注册表存在 Python `cluster_config`、TypeScript JSON 和内置 Python 列表多条路径，尚无统一事实源。
- 当前 `model_sync.py` 解决的是主节点向 PyTorch Worker 同步同一工件，不等于面向用户的 Hub 下载、许可证确认和多模型部署控制器。
- 当前数据库不可用时，部分域回退内存或 JSON；节点、模型、配置、下载任务和会话没有统一的本地事务语义。
- 当前从节点首次接入已具备 Tailscale 基础，但没有多集群档案、主节点端点编辑、连接测试和显式切换体验。
- 当前 GGUF 可以本地完整推理，但不能参加 PyTorch hidden-state 层流水线。
- 当前 SD 1.5 是独立侧车，不得被普通 LLM 目录扫描器误注册为文本模型。

### 3.3 与微服务改造的关系

本计划按目标拓扑设计，不继续扩写即将退役的旧控制面：

- `control-svc`：模型目录、下载任务、集群档案、本地数据库和远端同步。
- `scheduler-svc`：节点选择、模型放置计划、Worker 分发与参与证明。
- `inference-svc`：模型检查、试加载、引擎健康、预热、卸载和推理。
- `api-gateway`：对 PC/Android/TUI 提供稳定 API、SSE 进度和鉴权入口。

在[微服务架构改造计划](archive/微服务架构改造计划.md)的清理门通过前，只做接口和数据模型设计，不把新能力同时实现到 Python 单体和 TypeScript 控制面两套事实源中。

---

## 4. 目标用户流程

### 4.1 添加集群

```text
打开“集群” -> 添加集群
  -> 输入档案名称
  -> 输入主节点 Tailscale IP 或 MagicDNS 名称
  -> 测试 /api/bootstrap/info
  -> 显示 cluster_id、主节点名、版本和证书/共享密钥状态
  -> 用户确认
  -> 完成 first-connect 并保存本机 cluster profile
```

“登记 Tailscale 地址”只登记已加入 tailnet 的 QLH 主节点端点，不调用 Tailscale 管理 API，也不替用户把设备加入 tailnet。

### 4.1A 首次登录、Auth App 注册与 Tailscale 绑定

QLH 的本地登录和 Tailscale 的网络身份必须分开。Tailscale 官方明确说明其自身不是身份提供方、没有 Tailscale 密码；因此“已有 Tailscale 账号能登录 QLH”在本计划中解释为完成 Tailscale 授权/绑定后进入 QLH，QLH 的会话认证仍由 Auth App/TOTP 完成，不收集 Tailscale 或第三方 IdP 密码。

```text
首次打开主节点
  -> 在本机创建 owner 用户（用户名/显示名即可，不要求 email）
  -> 生成一次性 TOTP 密钥：Base32 字符串 + otpauth:// URI + 二维码
  -> 用户用 Aegis 或其他兼容 Auth App 扫码/手工录入
  -> 输入首个 6 位验证码确认，随后显示一次性恢复码
  -> 选择“已有 Tailscale”或“创建 Tailscale tailnet”
       已有：打开官方登录/授权页或本机 tailscale up，用户用自己的 IdP/passkey 完成认证
       没有：跳转官方 Get Started/注册流程，完成后回到 QLH
  -> QLH 读取本机 Tailscale 状态，或在可选 OAuth 授权路径拿到一次性设备授权
  -> 绑定 tailnet_id + tailscale_user_id + 绑定节点 ID，完成本地用户创建
```

Tailscale 的 `loginName` 可能是 emailish 字符串，只作展示和诊断，不作 QLH 主键；绑定主键使用 Tailscale API 的稳定 `user.id` 与 `tailnetId`。QLH 不为用户代注册第三方 IdP，也不代管 Tailscale 账号。

### 4.1B 用户更换 tailnet

更换网络必须由当前用户在主节点发起，并用当前 TOTP 或恢复码重新认证。主节点先建立 `pending` 新绑定，要求用户在新的 tailnet 完成官方授权，再原子地撤销旧 token/节点授权、切换 `active_binding` 并记录审计事件；新授权失败时保留旧绑定，不进入半切换状态。历史绑定只保留不可用的审计摘要，不允许旧 tailnet 继续访问 QLH。

### 4.1C 手动加入已有 QLH 集群（`CLUSTER-JOIN-S0`，规划）

用户可输入对方集群主节点地址并申请加入；不论本机此前是 provisional master 还是普通节点，成功后均只取得 `client` 身份。本机必须显式确认降级；已确认 master 不因收到远端地址自动降级。目标节点本地生成 Ed25519 密钥对，管理员以 Auth App/TOTP 批准后签发短期、单次、绑定目标公钥的 `client-only` grant。

二维码和字符串只传递签名 grant 或一次性兑换引用，绝不传递长期 `cluster_secret`、TOTP seed、恢复码或私钥。可选管理员邮件先传递与 request digest 绑定的批准指令（例如 `Y <digest>`）；批准后可自动回信同一张短期、单次、绑定目标公钥的二维码/字符串，但不能下发长期秘密。无 SMTP/IMAP 时仍可在主节点 Web/TUI 批准。过期、重放、错公钥、错角色和错签名一律 fail-closed；失败不得删除用户数据、模型资产或现有身份。

详细状态机、票据字段和后续验收见《集群接入稳定性与本地RAG实施计划》§2。本票不依赖 Tailscale OAuth，也不改变 `MF-AUTH-N2` 的本地绑定/换网边界。

### 4.2 一键获取模型

一键部署的模型**地址与源由开发团队提供默认源**（随发布版本内置的源清单 + curated recipe，见 §7.1），用户零配置即可使用；也**允许用户自己更换源**（见 §4.3）。

```text
模型 -> 模型目录 -> 选择模型/量化档
  -> 展示默认源（开发团队 curated recipe：endpoint、来源、revision、许可证）
  -> 展示下载大小和设备适配结论
  -> 用户确认（或更换源后重新 resolve 再确认）
  -> 主节点下载到 staging
  -> 校验文件、摘要、格式和架构
  -> inference-svc 隔离试加载
  -> 注册不可变 artifact
  -> 生成部署计划
  -> 分发/预热选定节点
  -> 达到激活门后切换别名
```

默认源由开发团队提供并随版本更新，包含：

- 默认 endpoint（Hugging Face / ModelScope 官方地址与可配置镜像回退）；
- 固定 revision 的 curated recipes（Qwen/DeepSeek 等，见 §7.5）；
- 许可证、gated 状态与设备适配提示。

用户更换源后，下载前展示的 source 以实际使用的源为准，并在 manifest 中记录（§6.2）。

### 4.3 用户自定义来源与更换源

支持三种输入：

1. Hugging Face 仓库：`repo_id + revision + 可选文件/量化档`。
2. GGUF：Hugging Face 仓库中的单个或分片 GGUF，或本地 GGUF 路径。
3. 本地目录：完整 Transformers/Safetensors 目录；后续支持 Diffusers pipeline、LoRA 和 ControlNet 专用入口。

除输入自定义仓库外，用户还可以**更换下载源**（端到端配置见 §7.1）：

- 在“源管理”中新增/编辑/禁用源（endpoint base URL、token、优先级），默认源清单由开发团队提供，可一键恢复出厂；
- 更换源后必须**重新 resolve 并锁定新源返回的 commit revision**，不得跨源沿用旧摘要或旧 manifest；
- 下载前界面展示当前实际使用的源与 endpoint，用户必须先看到检查结果，再允许“部署”。

未知架构、缺 tokenizer、缺 chat template、需要未批准远程代码或摘要不完整时，界面只能显示“待适配/不可部署”，不能盲目调用 loader。

---

## 5. 总体架构

```text
PC / Android / TUI
        |
        v
api-gateway
        |
        +--------------------+
        |                    |
        v                    v
control-svc             scheduler-svc
  Model Catalog            Placement Planner
  Pull Job                 Worker Distribution
  Artifact Registry        Activation / Rollback
  Cluster Profiles              |
  Local SQLite + Outbox          v
        |                   inference-svc / Worker
        |                     inspect / load / warmup
        v
optional PostgreSQL projection

Artifact bytes:
staging -> blobs/sha256/<digest> -> manifests/<model>/<tag>.json
```

职责必须保持单向：

- control-svc 决定“有哪些工件、任务和集群档案”。
- scheduler-svc 决定“哪些节点部署、哪些节点实际参与”。
- inference-svc 决定“该工件能否由指定引擎加载”。
- Worker 不访问远端 PostgreSQL，不保存数据库密码，也不自行从公开 Hub 选择模型版本。

Hugging Face 下载使用独立 `model-fetcher` 工具进程和专用工具环境，由 control-svc 通过结构化内部协议驱动。该环境只包含 `huggingface_hub`、Safetensors/GGUF 元数据工具和下载依赖，不导入或升级推理环境中的 PyTorch、Transformers、Diffusers、bitsandbytes；真正的 loader 验证仍由对应 runtime profile 的 inference-svc sidecar 完成。

---

## 6. 模型目录与工件身份

### 6.1 两层目录

模型目录分为两层：

| 层 | 内容 | 是否可变 |
|---|---|---|
| Curated Catalog | 项目维护的模型配方、来源、兼容性、许可证提示和推荐设备；每个 recipe 绑定**开发团队默认源**（endpoint + 固定 revision），随发布版本更新 | 可通过有版本的 catalog 更新 |
| User Registry | 用户实际导入的模型、别名、参数和部署策略；条目可**更换源**（默认源 → 用户自定义源），历史 artifact 不原地修改 | 用户可增删；历史 artifact 不原地修改 |

目录条目不能直接指向“main 最新版”作为运行身份。解析下载前可以使用 branch/tag，下载完成后必须记录 Hub 返回的完整 commit revision 和 QLH 聚合 SHA256。

### 6.2 工件清单

建议 manifest：

```json
{
  "schema_version": 1,
  "artifact_id": "sha256:<aggregate_digest>",
  "source": {
    "provider": "huggingface",
    "repo_id": "Qwen/Qwen2.5-7B-Instruct",
    "requested_revision": "main",
    "resolved_revision": "<full_commit_hash>"
  },
  "format": "safetensors",
  "engine": "pytorch_transformers",
  "family": "qwen2",
  "files": [
    {"path": "config.json", "size": 0, "sha256": "..."}
  ],
  "capabilities": {
    "full_worker": true,
    "pytorch_layer_pipeline": false,
    "llama_cpp": false,
    "task_stage": true
  },
  "requirements": {
    "min_ram_bytes": 0,
    "min_vram_bytes": 0,
    "disk_bytes": 0,
    "runtime_profile": "llm-cuda-v1"
  },
  "license": {
    "id": "...",
    "acceptance_required": false
  }
}
```

manifest 还应记录 tokenizer、chat template、上下文上限、量化方式、分片关系、adapter/base 依赖、信任策略和适配器版本。不得把访问 token、集群密钥或数据库密码写入 manifest。

### 6.3 内容寻址存储

建议布局：

```text
<QLH_MODEL_STORE>/
  staging/<job_id>/
  blobs/sha256/<first-two>/<digest>
  manifests/<namespace>/<name>/<tag>.json
  deployments/<node_id>/<artifact_id>.json
  quarantine/<job_id>/
```

要求：

- 下载只写 staging；全部校验通过后用同一文件系统内原子 rename 提交。
- 相同 SHA256 blob 去重；删除别名只减少引用，垃圾回收另行执行。
- GC 不删除 active、rollback、正在分发或未完成 outbox 引用的工件。
- 模型目录必须位于本地文件系统。不要把 SQLite、staging 或活跃模型映射到 SMB/Tailscale 网络共享盘。
- 启动时扫描不直接信任文件名；先读 manifest，再核对实际文件摘要。

---

## 7. 下载与来源适配器

### 7.1 来源配置：开发团队默认源与用户更换源

**默认源由开发团队提供**，随发布版本内置，保证用户开箱即用的一键部署：

- **默认源清单**：每个发布版本内置 `sources.json` 之类的源清单，至少包含 Hugging Face 官方（`https://huggingface.co`）、ModelScope（`https://modelscope.cn`）以及可选的国内镜像回退（如 `https://hf-mirror.com`）；解析器/下载器通过注入 endpoint 使用当前源。模型网络层按 `QLH_HTTP_PROXY > 用户持久化配置 > 直连` 选择代理，不沿用或修改 `HF_ENDPOINT`、系统代理和全局 Hugging Face 配置。
- **默认源优先级**：同一模型可配置多源回退顺序（如官方源 → 镜像源），源不可用时按优先级自动回退；回退后必须重新 resolve，锁定回退源返回的 revision。
- **curated recipes 绑定默认源**：recipe 固定绑定开发团队默认源与固定 revision（见 §7.5 与 `control/src/data/curated-catalog.ts`），解析/下载全程可复现，用户无需填写任何地址即可一键部署。

**用户允许自己更换源**：

- **源管理**：设置中提供“源管理”（新增/编辑/禁用源），每个源包含：名称、endpoint base URL、访问 token（凭据库引用，不落库不落日志）、优先级、启用状态；用户可将任一源设为当前默认源，也可一键恢复开发团队默认源。
- **更换源流程**：更换源后，该源下的一键部署必须重新执行 resolve（dry-run）→ 展示新源返回的 revision/文件清单/大小 → 用户确认 → 下载；**不得跨源沿用旧 digest、旧 manifest 或旧 staging 文件**，manifest 的 `source` 记录实际使用的 provider/endpoint/revision（§6.2）。
- **约束**：更换源只改变“从哪里取”，不改变校验、试加载、注册与分发流程；自定义源与默认源走同一套准入（未知架构 fail-closed，`trust_remote_code` 恒 false）。

### 7.2 Hugging Face 适配器

使用 `huggingface_hub` 的结构化 API，不再拼接 shell 命令：

- `snapshot_download()`：下载完整 Transformers/Diffusers 仓库。
- `hf_hub_download()`：下载指定 GGUF、配置或 adapter 文件。
- `revision=<完整 commit>`：创建可复现工件。
- `allow_patterns` / `ignore_patterns`：避免把无关框架权重全部下载。
- dry-run：下载前计算实际缺失文件和空间需求。
- 独立 `cache_dir`：不得修改用户全局 `HF_HOME` 或项目全局 Python 解释器。

私有或 gated 仓库：

- 用户在本机完成授权并输入 token。
- token 存入操作系统凭据库；开发模式可使用进程环境变量。
- 数据库和日志只保存 credential reference，不保存 token 明文。
- 用户必须显式确认仓库许可证/访问条款；QLH 不绕过 gated model 审批。

### 7.3 GGUF 适配器

GGUF 不是“任意单文件都能跑”。适配器应解析：

- `general.architecture`、模型名称和参数量。
- `general.file_type` / quantization。
- tokenizer、chat template、context length 和 rope 参数。
- 分片编号与总数。
- 多模态 projector 或 adapter 依赖。

随后使用项目锁定 commit 的 llama.cpp 做 `load -> tokenize -> short generate -> unload` 冒烟。只有当前构建明确支持该 architecture，才标记 `llama_cpp=true`。

llama.cpp 的 `-hf` 能力可以作为调研和命令行兜底，但正式 QLH 部署仍应先落入自身内容寻址仓库，避免各 Worker 各自解析“latest”并得到不同工件。

### 7.4 Safetensors/Transformers 适配器

通用入口只负责读取 AutoConfig、tokenizer 和静态元数据；真正加载由 family adapter 决定：

| 适配等级 | 含义 |
|---|---|
| `catalog_verified` | 项目固定 revision 并完成真实加载/生成测试 |
| `family_supported` | 架构属于已支持 family，但该具体工件只完成静态检查 |
| `full_worker_only` | 可完整推理，不支持 PyTorch 层拆分 |
| `inspection_only` | 可识别，但尚不能部署 |
| `rejected` | 格式、依赖、远程代码或安全检查不通过 |

默认 `trust_remote_code=false`。确需远程代码的老模型只能使用经过项目审查、锁定 revision 和文件摘要的 curated recipe，并在隔离 sidecar 中运行；用户填写任意仓库时不得自动执行仓库 Python 文件。

### 7.5 第一批适配矩阵

| 模型族/格式 | 首期引擎 | 首期目标 | 分布式边界 |
|---|---|---|---|
| Qwen-1.8B Safetensors | PyTorch | 保持现有兼容、纳入统一 manifest | 层流水线需全节点同 digest |
| Qwen2.5 7B/14B Safetensors | PyTorch | 完整 Worker + family adapter | 层流水线另行真实准入 |
| DeepSeek-R1-Distill-Qwen 1.5B/7B/14B/32B | PyTorch | 完整 Worker + tokenizer/chat 检查 | 不因同属 Qwen 系自动宣称层拆分通过 |
| Qwen/DeepSeek GGUF | llama.cpp | PC/Android 完整推理 | 同一执行池使用相同 GGUF digest；不进入 PyTorch 层流水线 |
| 任意已支持 architecture 的 GGUF | llama.cpp | 静态检查 + 本机冒烟后注册 | 默认完整 Worker/任务 Stage |
| SD 1.5 Diffusers | diffusion sidecar | 保留独立图像模型入口 | 不进入 LLM 目录或文本模型切换器 |

后续增加 Llama、Mistral、Gemma、embedding、reranker 和多模态模型时，必须分别补 family adapter、固定工件和端到端测试；不能以 Transformers 能 `from_pretrained` 为由自动宣称全部模型受支持。

### 7.6 NVIDIA UVM、显存分页与主动预取调研（2026-08-09）

**结论：主动预取值得做，但当前 Windows 产品路线不应以 UVM 实现。** 对本项目当前 RTX 4060 Laptop 8 GB / Windows WDDM 环境，UVM 超额订阅是硬性 `No-Go`。MF-MEM-N1 已证明 pinned host memory + `cudaMemcpyAsync` 的 raw transport 明显优于 pageable，但 WDDM 下双/三槽 event 排程未形成足以抵消长尾的吞吐收益；下一步只允许 64 MiB、双槽、隔离 sidecar 的条件验证，三槽冻结。原生 Linux 上的 UVM 主动预取保留为限时 `Conditional Go`，没有数据前不得设为默认 allocator。

| 路线 | 当前决策 | 原因 |
|---|---|---|
| Windows WDDM `cudaMallocManaged` 超额订阅 | `No-Go` | 本机 `managedMemory=1`，但 `concurrentManagedAccess=0`；CUDA 12.6 规定 GPU oversubscription、GPU 目标 `cudaMemPrefetchAsync`、`SetPreferredLocation(GPU)` 和 `SetAccessedBy(GPU)` 均要求该属性非零 |
| WSL 2 UVM | `No-Go` | NVIDIA 明确说明 WSL 2 不提供 Full Managed Memory、并发 CPU/GPU access 不受支持，可能出现高系统内存占用和性能下降；不能把 WSL 当成绕开 WDDM 的 UVM 路径 |
| Windows 显式 pinned-memory 分层 pager | `Conditional Go（N2 隔离侧车）` | pinned raw H2D/D2H 已证明；双槽 pipeline 吞吐只提升 0.56%-1.02% 且 P95 stall 约为单槽 1.85-1.98 倍，因此先验证容量和真实层执行，不设为默认 |
| Windows 三槽 pager | `No-Go（冻结）` | 相对单槽 pipeline 为 -0.88%-+1.20%，P95 stall 达 2.72-2.97 倍，且 pinned/VRAM 固定池扩大到三倍 |
| 原生 Linux UVM demand paging | `No-Go（只作负对照）` | PyTorch 2.13 官方不建议 DL 工作负载使用 UVM；显存饱和后会先逐出到 CPU、再搬入新页，形成昂贵双向传输和长尾停顿 |
| 原生 Linux UVM + 主动预取 | `Conditional Go（限时 PoC）` | 仅在 `concurrentManagedAccess=1`、32 GB+ RAM、固定模型和 Nsight 证据齐备时评估；必须胜过或接近显式 pager，否则冻结 |
| KV cache host offload / paged attention | `独立候选` | KV 的生命周期、复用和逐请求淘汰与只读权重不同；先采用块级显式管理，不把 KV 与权重一起交给 UVM |

#### 7.6.1 本机事实与容量下界

本轮只做只读能力探测和临时内存微基准，不修改源码：

- GPU：`NVIDIA GeForce RTX 4060 Laptop GPU`，compute capability 8.9，物理显存 8,585,216,000 bytes（`nvidia-smi` 显示 8,188 MiB），驱动模式 `WDDM`；PyTorch 2.13.0+cu126 使用 CUDA Runtime 12.6。
- CUDA 属性：`managedMemory=1`、`pageableMemoryAccess=0`、`concurrentManagedAccess=0`、`pageableMemoryAccessUsesHostPageTables=0`、`directManagedMemAccessFromHost=0`。因此“能分配 Managed Memory”不等于“能按需分页或超额订阅”。
- 4 MiB `cudaMallocManaged` 临时分配成功；对 GPU 0 调用 `cudaMemPrefetchAsync` 和 `cudaMemAdviseSetPreferredLocation` 均返回 `cudaErrorInvalidDevice (101)`，与 Runtime API 的属性前置一致。
- 系统 RAM 总计 16,782,925,824 bytes，本轮探测时空闲约 5.42 GB。Windows pagefile 不能替代 pinned/managed RAM：换页会叠加磁盘长尾，且 WDDM UVM 本身不允许超过物理显存。
- 256 MiB pinned buffer、12 轮临时拷贝的单次本机测量为 H2D 12.142 GB/s、D2H 13.033 GB/s。按此计算，每 token 每新增 1 GiB 冷权重至少增加约 88 ms H2D 下界；若发生 UVM 逐出再搬入，互连流量与停顿还会更高。7B BF16 若每 token 需要搬运约 14 GB 权重，仅 H2D 理论下界就约 1.15 s，尚未包含计算、映射、kernel、KV 和调度。

UVM 不增加物理总容量，只统一地址和迁移机制。当前 16 GB RAM + 8 GB VRAM 也不足以把 7B BF16、OS、CUDA context、KV cache 和临时张量作为安全常驻组合；7B BF16/14B UVM 真模型基准必须使用 32 GB+ RAM 的原生 Linux 主机。当前 Windows 可先用合成只读层、7B INT8/NF4 或 14B NF4 做显式 pager 容量/时延验证。

#### 7.6.2 API 语义与不能混淆的边界

| API / hint | 实际作用 | 本项目约束 |
|---|---|---|
| `cudaMemAdviseSetReadMostly` | 允许处理器建立只读副本；写入会使其他副本失效 | 只适用于冻结权重；KV cache、activation、量化临时 buffer 不设置 |
| `cudaMemAdviseSetPreferredLocation` | 只给迁移策略一个偏好，不立即迁移；GPU preferred 要求 `concurrentManagedAccess=1` | 不对全模型设 GPU preferred；官方说明该 hint 可能覆盖防抖策略并导致页面持续 thrash |
| `cudaMemAdviseSetAccessedBy` | 预建页表映射、减少首次 fault，不决定物理驻留，也不搬数据 | 可与 CPU preferred 组合做 Linux zero-copy/冷层负对照；不能冒充 prefetch |
| `cudaMemPrefetchAsync` | stream-ordered 地请求将范围迁往 CPU/GPU；后续同 stream 操作会等待其完成 | 只在 capability gate 通过后使用；下一层在 copy stream 预取，compute stream 用 event 等待，不依赖首次 kernel fault |
| `cudaMemcpyAsync` + pinned host memory | 显式维护 host/device buffer 并调用 copy engine | 当前 Windows 主路线；实现较繁琐，但数据移动、双缓冲和失败点最可控 |
| PyTorch `CUDAPluggableAllocator` / `torch.cuda.MemPool` | 可在 C/C++ allocator 与局部 memory pool 中尝试不同 CUDA 分配器 | 不全局替换现有 caching allocator；UVM 只在隔离 sidecar 和选定权重 pool 内实验 |

主动预取只能去掉 GPU 缺页处理、重复 fault、页表更新和不可预测调度的一部分开销，**不能消除 PCIe 必须搬运的权重字节**。NVIDIA 的连续访问实验中，on-demand migration 在 PCIe 上约 5.4 GB/s，预取约 10.9 GB/s，显式 copy 约 11.4 GB/s；另一组 oversubscription 实验显示结果会随平台、超订比例和访问模式相差最高 100 倍，随机访问在 x86 fault-driven 路径可跌到数百 KB/s。对顺序 Transformer 层，预取有机会接近 bulk copy；对 batch=1 自回归 decode，每层计算通常不足以完全掩盖搬运，DeepSpeed ZeRO-Inference 的短序列实验也未观察到 CPU offload prefetch 收益。

#### 7.6.3 首期 pager 设计边界

首期只分页**不可变权重**，不分页 KV cache、activation、CUDA graph workspace 和量化 scratch：

```text
CPU pinned / managed read-only layer store
        |
        | copy/prefetch stream: layer i+1
        v
GPU slot A/B  <---- event ---->  compute stream: layer i
        |
        +---- release/discard previous layer only after last-use event
```

1. 启动时冻结层顺序、每层字节数、不可拆模块、常驻热模块和 GPU budget；embedding、首尾层、lm_head、KV/activation 优先留在 GPU。
2. 首期显式 pager 只使用 2 个固定 device slot 和 64 MiB tile；host staging 必须 pinned，并由有界预取队列填充下一 tile，再以独立 copy stream 发起 H2D。只有对应 event 完成后 compute stream 才能执行；三槽除非新证据推翻 N1 长尾结论，否则不得恢复。N2 侧车已实现该队列，但 WDDM overlap 仍为 0%，因此只证明生命周期/正确性，不提升为默认吞吐策略。
3. Linux UVM 变体按层或连续 tile 建立 managed range：冻结权重设 `ReadMostly`，冷层以 CPU preferred + GPU `AccessedBy` 做 zero-copy 负对照，工作层用 `PrefetchAsync` 主动迁移。不得给全模型设置 GPU preferred。
4. 不在 forward 中静默切换策略。runtime profile 必须显式为 `resident`、`accelerate_auto`、`explicit_layer_pager`、`uvm_prefetch` 或 `uvm_demand_negative_control`；manifest/runtime record 写入 OS、driver mode、CUDA 属性、tile 大小、prefetch distance 和 host/device budget。
5. 任一 capability 不满足时排队前返回 `resource_rejected/unsupported_runtime_profile`；Windows/WSL 不能偷偷退回 demand paging。

#### 7.6.4 分期任务与决策门

| 票号 | 状态 | 内容 | 完成判据 |
|---|---|---|---|
| **MF-MEM-N0** | `Completed（文档与本机能力门，2026-08-09）` | 官方资料交叉核对；查询 WDDM/CUDA 属性；验证 GPU prefetch/advise 返回；测 pinned H2D/D2H 下界 | 明确 Windows/WSL UVM `No-Go`、显式 pager `Go`、Linux UVM `Conditional Go`，不提交运行时代码 |
| **MF-MEM-N1** | `Completed（Windows 真实 CUDA transport 门，2026-08-09）` | 独立 harness 固定 64/256/1024 MiB tile，比较 pageable、pinned 单槽、双槽和三槽；两轮 EX-N1 矩阵均 12/12 | H2D/D2H、重叠率、P50/P95/max stall、显式 pinned/VRAM 峰值和 checksum 均落盘；不修改生产 runtime |
| **MF-MEM-N2** | `Completed（合成 sidecar gate，2026-08-09）` | 在隔离 PyTorch family sidecar 实现 64 MiB/双槽 `explicit_layer_pager`；固定 host/device slot、显式 copy/compute event、有界 host 预取队列、取消与回收；不实现三槽、不接生产 runtime | `plan-model-fleet-explicit-pager-v1` 四单元 `4/4 passed`；CUDA batch=1 与 throughput batch=4 均输出 digest 一致，取消在第 2 层停止且释放后 allocated/reserved 为 0，CPU control equality 通过；Windows WDDM overlap=0%，不宣称吞吐收益 |
| **MF-MEM-N2R** | `Completed（真实 7B NF4 基线与模块级适配器，2026-08-09）` | 固定 `deepseek-r1-distill-qwen-7b` Safetensors（15,231,233,024 bytes）上运行 NF4；比较 resident、`device_map=auto`，并以显式 module move hook 做真实层 page-in/page-out；不把 BF16 原始文件冒充独立 INT8/NF4 工件 | artifact gate `3/3 passed`；真实对照 `6/6 passed`：context=128 batch=1 resident/auto tok/s `4.62/4.54` 且 digest 一致，context=2048 resident batch=1 `2.23 tok/s`、auto batch=4 `1.76 tok/s`；显式 hook 28 层进出各 28 次、H2D/D2H 各 3.039 GiB；CPU-only 因需约 20.4 GB 可用 RAM 而当前约 10.2 GB 明确拒绝；固定 tile 作为 N2F 独立门 |
| **MF-MEM-N2F** | `Completed（真实固定 tile/取消/长输出门，2026-08-09）` | 在真实层 hook 前增加固定 64 MiB、双槽 host/device bridge，显式分块搬运并支持 layer-boundary cancellation；ctx128 完整、ctx8192 取消边界及输出 32/128；仍不接生产 runtime | `plan-model-fleet-n2f-fixed-tile-v1` 为 `3/3 passed`：ctx128 完整 28 层进出、ctx128/8192 在首层回收后取消；补充输出 32/128 均完成。ctx128 输出 1/32/128 tok/s 约 `0.25-0.27/0.293/0.297`，128 token 约 431.7 s；固定 tile 分块 12/336/10,752/43,008，bridge 峰值模块 116.5 MiB，VRAM allocated/reserved 约 2.75/5.63 GiB。桥接只限制单次传输块，不限制完整模块驻留；未宣称吞吐收益 |
| **MF-MEM-N2P** | `Completed（Windows 真实延迟/预取决策，2026-08-09）` | 对真实 7B 显式 pager 比较预取距离 0/1、host staging overlap、CUDA stream overlap、TTFT/inter-token P50/P95/max 和 resident NF4 基线；不在当前 Windows WDDM 路线启用 UVM | `plan-model-fleet-n2p-latency-v1` 为 `3/3 passed`，四种结果 digest 均为 `e958d843…`；resident TTFT/inter-token P95/max `1.403s/78.5/80.0ms`，serial pager `3.739s/3557.6/3576.8ms`，distance=1 `3.989s/3429.6/3438.6ms`。distance=1 host staging overlap `100%` 但 CUDA H2D overlap `0%`，E2E 比 serial 约慢 `1.0%`、TTFT 约慢 `6.7%`，P95 仅改善约 `3.6%`；取消回收补充门通过 | `Freeze` 双槽 host prefetch overlap 为默认优化；保留显式 pager 容量 profile。下一票仅在原生 Linux + 32 GB RAM 条件满足时进入 N3 UVM 能力门 |
| **MF-MEM-N3** | `Blocked（需原生 Linux + 32 GB RAM）` | 仅当 `concurrentManagedAccess=1` 时实现局部 UVM pool；比较 demand、ReadMostly+prefetch、CPU preferred zero-copy 和显式 pager | Nsight 给出 fault groups、H2D/D2H、迁移/逐出和 stream overlap；未通过属性门则记 `unsupported`，不得模拟通过 |
| **MF-MEM-N4** | `Planned（决策票）` | 按真实 7B BF16/14B 候选和固定上下文矩阵作 Promote/Freeze | UVM 只有同时满足下列门才 Promote；否则冻结 UVM，保留显式 pager/量化/KV 专用路线 |

MF-MEM-N1 使用 `scripts/model_fleet_memory_transport.py` 和 `fixtures/experiment-plans/plan-model-fleet-memory-transport-v1.json`，在 RTX 4060 Laptop 8 GB、驱动 610.47、Torch 2.13.0+cu126/CUDA 12.6、Windows WDDM 上以 20 ms 合成 compute、8 次正式样本和 2 次 warmup 执行。下表为加入 checksum 硬门后的第二轮 12/12 结果；`P50=0` 是 WDDM 批次排程下部分 tile 无等待，不代表没有长尾，必须同时看 P95/max。

| Tile MiB | 模式 | H2D / D2H GB/s | Pipeline GB/s | 重叠率 | Stall P50 / P95 / max ms | 峰值 pinned / VRAM MiB |
|---:|---|---:|---:|---:|---:|---:|
| 64 | pageable | 6.794 / 6.791 | 2.042 | 0.0% | 9.86 / 10.43 / 10.44 | 0 / 64 |
| 64 | pinned 单槽 | 11.804 / 12.705 | 2.318 | 0.0% | 6.12 / 6.29 / 6.31 | 64 / 64 |
| 64 | pinned 双槽 | 11.836 / 12.612 | 2.335 | 33.4% | 6.28 / 11.67 / 11.70 | 128 / 128 |
| 64 | pinned 三槽 | 11.833 / 12.620 | 2.346 | 33.6% | 0.00 / 17.13 / 17.13 | 192 / 192 |
| 256 | pageable | 6.976 / 6.332 | 4.394 | 0.0% | 37.95 / 39.81 / 40.08 | 0 / 256 |
| 256 | pinned 单槽 | 12.062 / 13.000 | 6.136 | 0.0% | 22.65 / 22.80 / 22.81 | 256 / 256 |
| 256 | pinned 双槽 | 12.053 / 13.055 | 6.199 | 12.2% | 22.54 / 44.67 / 44.70 | 512 / 512 |
| 256 | pinned 三槽 | 12.077 / 13.005 | 6.083 | 11.7% | 0.00 / 66.74 / 66.76 | 768 / 768 |
| 1024 | pageable | 7.050 / 6.404 | 6.097 | 0.0% | 152.91 / 158.28 / 159.36 | 0 / 1024 |
| 1024 | pinned 单槽 | 12.153 / 13.091 | 10.469 | 0.0% | 88.82 / 89.33 / 89.39 | 1024 / 1024 |
| 1024 | pinned 双槽 | 12.160 / 13.105 | 10.529 | 13.0% | 89.21 / 176.89 / 176.93 | 2048 / 2048 |
| 1024 | pinned 三槽 | 12.172 / 13.074 | 10.514 | 13.8% | 0.00 / 265.28 / 265.43 | 3072 / 3072 |

**N1 决策**：pinned 单槽相对 pageable 的 H2D 为 1.724-1.737 倍、D2H 为 1.871-2.053 倍，transport 路线继续；双槽只作为 N2 的容量/正确性候选，不能按当前数据宣称有效隐藏传输；三槽冻结。N2 固定 64 MiB 双槽池（128 MiB pinned + 128 MiB device 起步），必须保留 pinned 单槽对照，并把真实层计算而非 20 ms 合成 kernel 作为 Promote/Freeze 依据。

**N2 合成侧车证据（2026-08-09）**：`scripts/model_fleet_explicit_layer_pager.py` 与 `fixtures/experiment-plans/plan-model-fleet-explicit-pager-v1.json` 在 Windows WDDM/RTX 4060 Laptop 8 GB、Torch 2.13.0+cu126/CUDA 12.6 上运行。`build/experiments/mf-mem-n2-windows-20260809-v2/summary.json` 显示 4/4 单元通过：CUDA batch=1（4 层）和 throughput batch=4 均 `output_equal=1`，峰值 pinned 为 128 MiB、峰值 device allocation 约 128 MiB；取消单元在第 2 层后返回 `cancelled=1`，关闭 pager 后 `released_vram_allocated_bytes=0`、`released_vram_reserved_bytes=0`；CPU control 也与 resident reference digest 一致。两项 CUDA 完成样本的 makespan 为 66.812/66.499 ms，WDDM event 时间线 overlap 均为 0%，所以本票只关闭固定容量、输出一致性、取消和资源回收门，不把双槽宣传为吞吐优化。专项测试 `tests/test_model_fleet_explicit_layer_pager.py` 为 6/6；未修改 `model_runtime_sidecar.py` 或生产 runtime。

**N2R 真实 7B NF4 证据（2026-08-09）**：固定工件为 `models/deepseek-r1-distill-qwen-7b`，Safetensors index 的 `total_size=15,231,233,024`，通过 `scripts/model_fleet_n2r_artifact_gate.py` 的 3/3 资源门；NF4 是固定 BF16 权重上的显式 bitsandbytes 配方，不登记为独立量化文件。`build/experiments/mf-mem-n2r-real-20260809-v2/summary.json` 的真实对照为 6/6：resident NF4 与 `device_map=auto` 在 context=128、batch=1 的 tok/s 为 4.62/4.54，输出 digest 相同；context=2048 resident batch=1 为 2.23 tok/s，auto batch=4 为 1.76 tok/s，峰值 allocated 分别约 5.789/6.808 GiB。显式 module move hook 在 28 层上 page-in/page-out 各 28 次，H2D/D2H 各 3.039 GiB，context=128、单 token 完成但 tok/s 仅 0.38；这证明真实层迁移 API 路径，不证明固定 64 MiB tile 或产品吞吐。CPU-only 因安全预算约需 20.4 GB 可用 RAM、当前约 10.2 GB 而明确 `resource_rejected`；N2F 继续验证固定 tile 与取消边界。专项测试 `tests/test_model_fleet_n2r_artifact_gate.py` + `tests/test_model_fleet_n2r_compare.py` 为 14/14，未修改生产 runtime。

**N2F 真实固定 tile 证据（2026-08-09）**：`scripts/model_fleet_n2r_compare.py` 的 `explicit-layer-pager-64` 使用 64 MiB host/device tile、2 个槽位，输出落在 `build/experiments/mf-mem-n2f-fixed-tile-20260809-v1/summary.json`，计划 `3/3 passed`。ctx128 完整生成 1 token 时 28 层 page-in/out 各 28 次、bridge 336 个分块、H2D/D2H 各 3.039 GiB；ctx128 与 ctx8192 均在 `cancel_after_layer=1` 后完成首层回收并返回 `cancelled=1`。补充输出 32/128 token 均完成，tok/s 分别为 0.293/0.297，128 token 约 431.7 s，层迁移分别约 104.4/417.7 GB（H2D 与 D2H 各计一次）。桥接器峰值模块为 116.5 MiB，VRAM allocated/reserved 约 2.75/5.63 GiB；因此 64 MiB 是单次搬运上限，不是完整模型显存上限，也没有形成吞吐优化结论。新增 `tests/test_model_fleet_n2f_fixed_tile.py` 与 N2R 回归共 9/9，未接生产 runtime；下一票为 N2P 预取/重叠和延迟决策。

**N2P 真实预取/延迟证据（2026-08-09）**：`build/experiments/mf-mem-n2p-latency-20260809-v1/summary.json` 的 `plan-model-fleet-n2p-latency-v1` 为 `3/3 passed`，固定同一 7B NF4 工件、ctx128、batch=1、输出 8。resident NF4 的 TTFT/inter-token P95/max 为 `1.403 s / 78.5 / 80.0 ms`，serial explicit pager 为 `3.739 s / 3557.6 / 3576.8 ms`；distance=1 后为 `3.989 s / 3429.6 / 3438.6 ms`。三者 output digest 均为 `e958d843…`。distance=1 的 host staging 与层计算区间重叠率为 `100%`，但实际 CUDA H2D stream overlap 仍为 `0%`；相对 serial pager，E2E 约慢 `1.0%`、TTFT 约慢 `6.7%`，inter-token P95 仅改善约 `3.6%`，不足以抵消复杂度。补充 `cancel_after_layer=1` 回收门通过。因此 N2P 结论为 `Freeze`：双槽 host prefetch 只保留审计/容量 profile，不进入默认吞吐路径；Windows/WSL UVM 仍不进入实现。

固定对照至少覆盖 context 128/2048/8192、输出 32/128 token、batch=1 与吞吐 batch；记录 TTFT、inter-token P50/P95/max、tok/s、CPU 占用、峰值 RAM/pinned/VRAM、H2D/D2H bytes、GPU page-fault groups、迁移次数、输出 digest/误差和取消后回收。不能只报告“成功加载”。

UVM Promote 门：同 artifact、同 GPU budget 下，`uvm_prefetch` 的 tok/s 不低于显式 pager 95%，P95 inter-token latency 不高于显式 pager 110%，最大停顿不超过中位数 2 倍，warmup 后目标层无 demand GPU fault，且实现复杂度或内存占用有可量化收益。任一不满足即 `Frozen`。显式 pager 本身若既不能比 CPU-only 提升至少 25%，也不能让更高质量模型达到可接受延迟，则不进入产品默认，只保留容量实验 profile。

#### 7.6.5 调研来源

- [NVIDIA CUDA Programming Guide：Unified Memory](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/unified-memory.html#um-legacy-devices)
- [NVIDIA CUDA 12.6 Runtime API：Memory Management](https://docs.nvidia.com/cuda/archive/12.6.0/cuda-runtime-api/group__CUDART__MEMORY.html)
- [NVIDIA CUDA on WSL：Known Limitations](https://docs.nvidia.com/cuda/wsl-user-guide/index.html#known-limitations-for-linux-cuda-applications)
- [NVIDIA：Maximizing Unified Memory Performance](https://developer.nvidia.com/blog/maximizing-unified-memory-performance-cuda/)
- [NVIDIA：Improving GPU Memory Oversubscription Performance](https://developer.nvidia.com/blog/improving-gpu-memory-oversubscription-performance/)
- [NVIDIA CUDA Samples：UnifiedMemoryPerf](https://github.com/NVIDIA/cuda-samples/tree/master/cpp/6_Performance/UnifiedMemoryPerf)
- [PyTorch 2.13 CUDA semantics：custom allocators / UVM note](https://docs.pytorch.org/docs/2.13/notes/cuda.html#mixing-different-cuda-system-allocators-in-the-same-program)
- [Hugging Face Accelerate：Big Model Inference](https://huggingface.co/docs/accelerate/main/en/concept_guides/big_model_inference)
- [DeepSpeed ZeRO-Inference：layer streaming and prefetch](https://www.deepspeed.ai/2022/09/09/zero-inference.html)
- [TensorRT-LLM：KV cache host offload example](https://github.com/NVIDIA/TensorRT-LLM/blob/main/examples/llm-api/llm_kv_cache_offloading.py)

---

## 8. 一键部署状态机

### 8.1 下载任务状态

```text
created
  -> resolving
  -> awaiting_license
  -> downloading
  -> verifying
  -> inspecting
  -> smoke_loading
  -> registered
  -> planning
  -> distributing
  -> warming
  -> active
```

终止状态：`cancelled`、`rejected`、`failed`、`quarantined`、`rolled_back`。

每次状态转换写入本地事务库，并附 `job_id`、幂等键、错误码、当前字节数、总字节数和可重试标志。服务重启后从最后一个持久状态恢复；不能只把进度保存在进程内。

### 8.2 部署必须分两阶段

1. **Prepare**：Worker 下载/接收工件，校验摘要，检查磁盘/内存/引擎，完成隔离试加载。
2. **Activate**：达到部署策略要求后，由 scheduler 一次性切换 active artifact/alias epoch。

部分节点 prepare 失败时不得偷偷把任务标成 active。系统应展示：目标节点、已准备节点、失败节点、实际激活策略、是否回退主节点。

### 8.3 激活与回滚

- active alias 指向 `artifact_id + deployment_epoch`，不修改原工件。
- 保留上一版 rollback 指针，直到新版本通过健康窗口。
- 运行中请求继续使用创建时锁定的 artifact；新请求使用新 epoch。
- 分层流水线中任何节点缺少相同 digest 时，整条流水线不得启动。
- GGUF 完整 Worker 可以按任务重派，但结果统计必须记录实际执行节点和 artifact digest。

---

## 9. 设备匹配与放置策略

用户点击下载前，系统先给出部署建议：

| 输入 | 决策用途 |
|---|---|
| 模型格式、参数量、量化、上下文 | 估算 RAM/VRAM/磁盘和引擎 |
| 节点 CPU/GPU、显存、内存、平台 | 过滤不可部署节点 |
| runtime profile 与依赖版本 | 避免把 CUDA 模型部署到集显包，或把未支持 GGUF 发给旧 llama.cpp |
| 网络 RTT/吞吐和剩余磁盘 | 决定主节点转发、P2P/主节点分发和并发数 |
| 分布式能力声明 | 区分层流水线、完整 Worker、任务 Stage、TP 孤岛和图像 Worker |

首期使用确定性规则，不用不透明评分替代硬约束。评分只在全部硬门满足后排序：

```text
compatible = format && engine && memory && disk && runtime && policy
score = compute_score - transfer_cost - load_cost - current_pressure
```

部署计划必须允许用户选择：仅主节点、推荐节点、指定节点、整个兼容执行池。Android Lite 默认不接收模型；Android Full 只有用户启用本地模型后才进入 GGUF 候选。

---

## 10. Tailscale 集群档案与主节点登记

### 10.1 Cluster Profile

客户端本地保存多个集群档案：

```json
{
  "profile_id": "home-lab",
  "display_name": "家庭集群",
  "cluster_id": "qlh-...",
  "master_host": "qlh-master.tailnet-name.ts.net",
  "master_api_port": 8000,
  "master_tcp_port": 8888,
  "node_id": "client-...",
  "last_verified_at": "...",
  "last_successful_endpoint": "..."
}
```

共享集群密钥由现有安全存储层保存，不能出现在 profile 导出、日志或二维码明文中。若当前产品继续接受“已进入 tailnet 即已由网络所有者审核”的风险模型，则不增加逐设备人工审批；但集群 HMAC 仍用于防止端口误连和跨集群串线。

### 10.2 地址规则

- 接受 Tailscale IPv4/IPv6 和 MagicDNS/FQDN。
- 主节点设置中区分 `advertised_tailnet_endpoint` 与 LAN 候选地址。
- first-connect 返回经过验证的 canonical endpoint；客户端保存最近成功端点。
- 多个主节点候选时由用户明确选择，不自动加入第一个扫描结果。
- 不信任任意 `X-Forwarded-For`；反向代理必须显式配置可信代理。
- 主节点登记不等于备用主节点提升。角色切换仍需 cluster epoch/fencing。

### 10.3 不依赖远端数据库发现

发现顺序调整为：

1. 当前 cluster profile 的最近成功端点。
2. 用户输入或固定 `QLH_MASTER_HOST`。
3. Tailscale peer/MagicDNS 候选探测。
4. 仅在用户显式启用兼容模式时查询用户自有的远端目录；默认不连接 PostgreSQL。

远端目录只能提供候选，不能覆盖已经通过 bootstrap 验证并保持 TCP 在线的主节点。远端出口离线不得让 UI 同时显示“TCP 已连接”和“主节点离线”，也不得阻塞 local-only 模式。

### 10.4 认证与 Tailscale 身份边界

- **QLH 身份**：由主节点本地 `user_id`、用户名/显示名、角色、状态和 Auth App/TOTP 凭据组成；不要求 email，不把 Tailscale `loginName` 当作登录名或唯一键。
- **Tailscale 绑定**：保存 `tailnet_id`、`tailscale_user_id`、绑定节点 ID、授权方式、绑定时间和撤销时间。`tailnet_id + tailscale_user_id` 是当前绑定的外部身份，名称/IP/MagicDNS 只作可变展示字段。
- **一一绑定约束**：一个 QLH 用户只能有一个 `active` Tailscale 绑定；一个主节点内一个 `tailnet_id + tailscale_user_id` 只能绑定一个 active QLH 用户。换网先完成新授权，再撤销旧绑定，历史记录不复活权限。
- **数据所有权**：`local_users`、TOTP 密钥引用、恢复码哈希、绑定记录和认证审计是用户主节点的本地事实源，不进入开发组服务、公共账号目录或远端 PostgreSQL projector。远端数据库即使由用户自行配置，也不接收这些认证秘密；备份只能是用户主动导出的加密包。

### 10.5 本地用户数据模型

建议在主节点 SQLite 增加独立认证域（与模型/任务域分离）：

```json
{
  "user_id": "qlh-user-...",
  "username": "local-name",
  "display_name": "可选显示名",
  "role": "owner|admin|member",
  "status": "active|suspended|revoked",
  "totp_secret_ref": "os-credential://qlh/totp/...",
  "totp_algorithm": "SHA1",
  "totp_digits": 6,
  "totp_period": 30,
  "recovery_code_hashes": ["argon2id-or-scrypt-hash", "..."],
  "tailscale_binding": {
    "tailnet_id": "stable-tailnet-id",
    "tailscale_user_id": "stable-user-id",
    "node_id": "node-id-or-null",
    "state": "active",
    "bound_at": "..."
  }
}
```

TOTP 原始密钥只通过 OS credential store/主节点密钥封装保存，数据库只存引用和算法参数；恢复码只存带盐哈希。认证日志记录用户 ID、结果、原因和时间，不记录验证码、密钥、恢复码或第三方 token。

### 10.6 Auth App 下发方式与实现选择

注册必须同时提供两种等价下发方式：

1. **字符串**：显示 Base32 secret，并提供可复制的 `otpauth://totp/...` provisioning URI；用户在 Auth App 中选择手工添加。
2. **二维码**：二维码只编码同一条 `otpauth://` URI，不在二维码中加入 QLH 之外的网络凭据；页面只显示一次，确认首个验证码后立即失效。

默认参数取兼容性优先的 TOTP SHA-1/6 位/30 秒，服务器允许一个有限时间步偏差并拒绝成功后的重复验证码；具体参数、随机密钥、密钥保护和安全存储遵循 RFC 6238。Aegis 作为推荐的外部 Auth App（支持 HOTP/TOTP、二维码和手工录入、加密 vault 与备份），QLH 不依赖其在线服务。

实现路线按风险排序：

- **默认**：QLH 实现标准兼容的 TOTP provisioning/验证，用户自行选择 Aegis、Google Authenticator、2FAS 等兼容客户端。
- **可选**：QLH 项目组维护自己的开源 Auth App，但必须另设 Android vault、密钥备份、升级签名和独立安全审计门。
- **不建议第一阶段**：直接复制/内嵌 Aegis 代码。Aegis 仓库声明 GPL-3.0，若形成衍生或再分发包，必须履行许可证义务并保留相应版权/许可证文本；把 Aegis 作为用户自行安装的外部应用不产生 QLH 运行时依赖。

### 10.7 Tailscale 官方能力与协议门

调研结论（2026-08-09）：

- Tailscale 官方身份文档明确其不是身份提供方，用户通过受支持的 IdP/SSO 或 passkey 注册/登录，不能把 Tailscale 账号密码交给 QLH。
- Tailscale OAuth clients 是 OAuth 2.0 **client credentials** API 访问令牌，按 scope 授权且 token 默认约 1 小时，不是通用用户登录协议。
- Tailscale “device provisioning with OAuth apps” 使用 authorization-code flow，但当前为 alpha，只允许 OAuth app 与授权用户处于同一个 tailnet；一次性 `auth_keys:create:once` 没有 refresh token，不能作为跨 tailnet 的全局 QLH 登录依赖。
- 因此默认实现不注册开发组统一 OAuth app，不把用户 token 上传开发组，也不依赖 Tailscale 商务合作；通过用户自己在官方页面/本机客户端完成授权，再由主节点保存最小绑定信息。

仅在未来需要自动创建设备、审计到个人用户或集中 OAuth 体验时，新增“每个用户 tailnet 自带 OAuth app”的实验适配器；正式发布前必须完成 Tailscale API 条款、alpha 能力、scope 最小化、token 存储和是否需要合作/批准的法务与合作方评审。跨 tailnet 切换不得假设单个 OAuth app 可复用。

### 10.8 第三方协议/许可证矩阵

| 第三方/能力 | 默认方案 | 是否需要项目组先谈协议 | 开放条件 |
|---|---|---|---|
| Tailscale 官方客户端、网页注册/登录、用户自有 tailnet | 由用户自行注册和授权，QLH 只保存最小绑定元数据 | 不以签署合作为前置；仍须遵守当前服务条款和 API 限制 | `MF-AUTH-N2` 只做真实用户授权与绑定，不代管密码 |
| Tailscale OAuth client / OAuth Apps | 不作为默认登录，`MF-AUTH-N3` 才评估 BYO app | 中央 app、白标、集中 token 或生产化 alpha 能力必须先做条款/合作/批准评审 | 最小 scope、单 tailnet、无 refresh token 和撤销流程可证明后才实验 |
| Aegis Authenticator（GitHub 仓库） | 用户自行安装的外部 Auth App；QLH 只实现标准 TOTP | 不调用 GitHub API，不以 GitHub 合同为前置 | 若复制、修改或再分发，必须履行 Aegis GPL-3.0；不把 Aegis 代码内嵌进第一阶段 QLH |
| GitHub OAuth 登录 | 不纳入当前需求；Tailscale 的 GitHub SSO 由用户自行选择 | 若未来加入 QLH 直接 GitHub 登录，需注册 OAuth App、审查条款/隐私和数据范围 | 未完成前不得把 GitHub 账号写成 QLH 登录主键 |
| RFC 6238 / `otpauth://` | 标准兼容实现和二维码编码 | 不需要商业协议 | 通过互操作、密钥保护、时间漂移和恢复门 |

---

## 11. 主节点 SQLite 事实源与远端数据库退场

### 11.0 存储主权与用户资产契约

QLH 的长期设计不是“开发组维护一个远端账号/数据库，用户只是客户端”。主节点才是用户的控制面和资产边界：

- 干净安装不要求 PostgreSQL、远端账号、外部数据库密码或开发组服务可达；`DATABASE_URL` 为空时仍必须完成首次登录、模型导入、Tailscale 绑定、任务恢复和本地推理。
- SQLite 保存所有核心事实：用户与 Auth App 引用、Tailscale 绑定、cluster/profile、模型 catalog/artifact 引用、pull/deployment、任务 journal、会话/设置、审计和备份索引。大模型 blob 仍落在用户指定的 artifact store，不上传开发组。
- 用户可以导出加密的主节点备份并在自己的新主节点恢复；备份密钥由用户掌握。开发组不拥有解密密钥，也不提供跨用户的中央恢复或远程重置。
- 远端 PostgreSQL 如继续存在，只能是用户明确配置的兼容出口或一次性迁移目标，必须显示 `legacy_remote_enabled`，不参与启动、登录、调度、资产读取和故障恢复。
- 远端退场后，删除 projector/outbox/远端凭据不会改变本地 SQLite、artifact store 或用户备份的语义。所有迁移必须先通过本地导出/恢复演练，再允许删除远端配置。
- “用户拥有资产”是 QLH 的存储和控制权原则，不替代 Hugging Face、ModelScope、模型作者或其他第三方许可证、版权和使用条款。

### 11.1 目标语义

在默认 `local_only` 模式下，主节点必须独立完成：

- 读取/保存集群设置和用户模型注册。
- 节点 bootstrap、节点目录和主节点自身身份恢复。
- 创建、恢复和取消模型下载任务。
- 保存 artifact、deployment、alias 和 rollback 状态。
- 本地会话、审查票和必要控制面事件持久化。
- 本地推理、完整 Worker 和任务图执行。

Worker 永远不直接访问远端 PostgreSQL。主节点通过 API/TCP 向 Worker 提供所需配置和工件身份；远端数据库即使启用，也不能绕过主节点控制面。

### 11.2 推荐架构：local-only，兼容导出隔离

```text
control-svc write
      |
      v
SQLite transaction: all core domain rows
      |
      +--> immediate local read
      |
      `--> optional user-triggered export --> legacy PostgreSQL / encrypted backup
```

选择 local-only 的原因：

- SQLite 与业务进程同机，启动、认证和恢复不依赖 DNS、Navicat、公网链路或开发组服务。
- 核心提交只需要一次本地事务，不因为远端网络抖动生成“本地成功、远端未提交”的双状态。
- 用户可以把加密备份放在自己的磁盘、NAS 或对象存储，远端 PostgreSQL 不是灾备前提。
- 兼容导出明确是用户主动操作，不得把它重新实现成第二个写入事实源。

这不是 SQLite/PostgreSQL 双主。默认不创建远端 projector/outbox；兼容期若启用导出，远端只接受带版本的用户主动导出，远端直接修改不回写主节点，也不能覆盖本地事实。

### 11.3 数据域划分

| 数据 | 本地 SQLite | 远端 PostgreSQL | 说明 |
|---|---|---|---|
| cluster/profile/settings | 权威 | 退场兼容导出 | 用户主节点控制网络和配置 |
| nodes 与能力快照 | 权威持久快照 | 退场兼容导出 | 高频 heartbeat 不依赖远端 |
| model catalog/registry/artifact | 权威元数据 | 退场兼容导出 | 权重只在用户 artifact store |
| pull/deployment jobs | 权威 | 退场兼容导出 | 支持本地重启恢复 |
| alias/activation/rollback | 权威 | 退场兼容导出 | 需 epoch/fencing |
| task journal | 保持独立 SQLite journal | **禁止默认导出** | 不把大 payload 写入远端 |
| conversations/reviews | 权威或按用户策略保存 | **禁止默认投影** | 内容留在用户主节点 |
| local_users/tailscale_bindings/auth_audit | 权威 | **禁止远端投影** | 只允许用户主动导出加密备份 |
| secrets/tokens/passwords | 不保存明文 | **禁止远端存储** | 使用 OS credential store |

### 11.4 建议表

- `schema_migrations`
- `clusters`、`cluster_endpoints`
- `nodes`、`node_capability_snapshots`
- `model_catalog_entries`、`model_recipes`
- `artifacts`、`artifact_files`、`artifact_refs`
- `pull_jobs`、`pull_job_events`
- `deployments`、`deployment_targets`、`model_aliases`
- `settings`、`sessions`、`messages`、`review_tickets`
- `local_users`、`tailscale_bindings`、`auth_audit_events`（认证域仅本地主节点）
- `outbox_events`、`projection_checkpoints`（仅兼容导出迁移；默认不启用）

所有本地实体使用稳定 UUID、`aggregate_version` 和 `updated_at`。兼容导出如启用，使用全局唯一 `event_id` 和 `aggregate_version`，但远端事件不得回写或覆盖本地主节点事实。

### 11.5 运行模式与健康状态

| 状态 | 含义 | 用户界面 |
|---|---|---|
| `local_only` | 用户主节点独立运行，未启用远端出口 | 默认正常；不显示远端故障 |
| `legacy_remote_enabled` | 用户主动开启远端兼容导出 | 显示“用户自管兼容出口”，不能阻塞本地 |
| `legacy_export_pending` | 兼容导出暂有积压 | 本地正常；显示待导出数量，不改变核心状态 |
| `legacy_export_recovering` | 兼容出口恢复，正在重试用户导出 | 本地正常；显示导出进度 |
| `local_readonly` | 本地磁盘/SQLite 写入失败 | 明确故障；禁止需要持久化的变更，推理按既有模型受控继续 |

不能继续使用笼统的“数据库无法连接，只能使用内存模式”覆盖所有情况。默认没有远端数据库；兼容出口失败只影响用户主动导出，本地持久化失败才是控制面故障。

### 11.6 SQLite 约束

- 只允许 control-svc 成为控制数据库写者；其他服务走内部 API，避免 Python/Node 多进程争抢写锁。
- SQLite 驱动必须先完成 Windows/Linux 打包取舍：固定 Node 运行时支持时优先评估内置 `node:sqlite`，否则评估原生驱动；任何方案都必须通过 SEA/随包 Node、安装、升级和卸载冒烟后才能冻结。
- 启用 WAL、busy timeout、foreign keys 和固定迁移版本；每次升级先执行 schema migration，再执行 `quick_check`。
- 数据库位于本地文件系统，不放网络文件系统。
- 定期执行 `PRAGMA quick_check`，使用 SQLite Backup API 创建用户可读的轮换快照，并提供加密导出/恢复演练。
- 磁盘不足、只读目录、损坏和迁移失败必须 fail closed，不回退为无提示内存写。
- task journal 保持独立数据库和实例命名空间，避免控制面大事务拖慢任务提交。
- 远端 PostgreSQL 相关配置、驱动和导出器必须是可选组件；主节点默认安装不应因缺少它们而启动失败。

### 11.7 主节点故障边界

本地数据库是默认事实源，但不自动解决“主节点整机损坏”。在没有用户主动创建可靠加密备份前：

- `legacy_export_pending` 状态下禁止把远端出口提升为备用主节点。
- 手工转移主节点前先导出并验证用户签名/加密快照；不要求等待远端数据库。
- 后续可增加主节点到用户自有备用主节点的加密快照复制，但不能把 SQLite 文件直接放到共享盘并双端打开。
- cluster epoch、master node ID 和 artifact alias epoch 必须一并转移，旧主节点恢复后不得继续接受写入。

---

## 12. API 草案

### 12.1 模型目录与工件

| API | 用途 |
|---|---|
| `GET /api/catalog/models` | 查询 curated catalog 与设备适配建议 |
| `POST /api/models/resolve` | 解析 HF/GGUF/本地来源，返回 revision、大小、许可和兼容性 |
| `POST /api/models/pull` | 创建幂等下载任务 |
| `GET /api/models/pull/{job_id}` | 查询状态与进度 |
| `GET /api/models/pull/{job_id}/events` | SSE 下载/校验/部署事件 |
| `DELETE /api/models/pull/{job_id}` | 取消；保留可恢复 partial 或按策略清理 |
| `GET /api/models/artifacts` | 列出本地工件、存储量和节点/profile 运行时准入汇总 |
| `POST /api/models/imports` | 从本机路径导入工件并执行运行时准入 |
| `GET /api/models/runtime-checks` | 查询本地运行时准入记录 |
| `POST /api/models/runtime-checks/retry` | 重新执行指定 manifest 的本机准入检查 |
| `DELETE /api/models/runtime-checks` | 标记指定运行时记录为 `stale` |
| `GET /api/models/pull` | 列出最近拉取任务及进度 |
| `GET/PUT/DELETE /api/models/network(/proxy)` | 查看生效代理、保存/清除用户级模型下载代理 |
| `GET /api/models/artifacts/{artifact_id}` | show：来源、摘要、格式、能力、部署和健康 |
| `DELETE /api/models/artifacts/{artifact_id}` | 引用检查后删除/进入 GC |
| `POST /api/models/deployments` | 创建节点部署计划 |
| `POST /api/models/aliases/{alias}/activate` | 原子激活或回滚 |

### 12.2 集群档案

| API | 用途 |
|---|---|
| `POST /api/cluster/profiles/verify` | 无副作用探测主节点身份和版本 |
| `POST /api/bootstrap/first-connect` | 首次接入并获得 cluster/node 配置 |
| `GET /api/cluster/endpoints` | 主节点查看/编辑自身 advertised endpoint |
| `POST /api/cluster/endpoints/verify` | 验证 Tailscale/MagicDNS/LAN 可达性 |

### 12.3 存储健康

`GET /api/storage/health` 应分开返回：

```json
{
  "local": {"status": "ok", "backend": "sqlite", "writable": true},
  "remote": {"status": "disabled", "backend": "postgresql", "mode": "legacy_export"},
  "export": {"pending_items": 0, "oldest_item_age_seconds": null},
  "effective_mode": "local_only"
}
```

旧 `/api/db/health` 在兼容期映射到该结构，不能再把远端 PostgreSQL 状态等同于整体存储状态；默认安装不创建远端连接或导出任务。

### 12.4 用户认证与 Tailscale 绑定

以下 API 只由当前主节点提供，认证域不进入远端 PostgreSQL projector；敏感字段只能在 loopback/已验证的主节点会话中返回一次：

| API | 用途 |
|---|---|
| `POST /api/auth/bootstrap` | 首次创建 owner，并一次性返回 TOTP 字符串、`otpauth://` URI 和二维码载荷 |
| `POST /api/auth/totp/verify` | 确认首次注册或换绑时的验证码；成功后立即使 bootstrap 包失效 |
| `POST /api/auth/login` | 用户名 + TOTP 登录，创建主节点本地会话 |
| `POST /api/auth/recovery-codes/rotate` | 当前 TOTP 二次确认后轮换恢复码，旧码全部失效 |
| `GET/POST/PATCH/DELETE /api/users` | 本地主节点用户列表、创建、角色/状态更新和撤销；不得返回 TOTP 原文 |
| `POST /api/tailscale/bind/prepare` | 读取本机 Tailscale 状态或生成官方授权跳转的 pending binding |
| `GET /api/auth/tailscale/local-status` | 在已认证主节点会话内只读读取本机 CLI 状态，并返回严格脱敏的绑定候选；不执行登录、换网或授权 |
| `POST /api/tailscale/bind/confirm` | 校验 `tailnet_id`、`tailscale_user_id`、节点 ID 并原子切换 active binding |
| `POST /api/tailscale/unbind` | 当前 TOTP/恢复码确认后撤销绑定和本地 token 引用 |
| `GET /api/tailscale/binding` | 返回当前绑定的脱敏状态、tailnet 展示名和最近验证时间 |

任何 `users`、`auth`、`tailscale/binding` 响应都不得返回密钥、恢复码、OAuth client secret、access token 或完整 `loginName`；导出功能单独生成用户主动触发的加密备份包。

---

## 13. 故障与恢复设计

| 故障 | 必须行为 |
|---|---|
| Hub/DNS/代理不可用 | 下载任务进入可重试状态，已激活模型继续运行 |
| 下载中断 | 使用来源 ETag/revision 与 partial 元数据续传；来源变化则重建 staging |
| SHA256 不匹配 | 移入 quarantine，不注册、不分发 |
| 文件名相同但内容变化 | 创建新 artifact，不覆盖旧 digest |
| 磁盘不足 | dry-run/配额阶段拒绝；不得下载到一半挤爆状态目录 |
| inference-svc 试加载崩溃 | sidecar 失败，主控制面继续运行；工件标记 load_failed |
| 某 Worker 分发失败 | 部署为 partial/failed；不伪报该节点参与 |
| 激活后健康失败 | alias epoch 回滚，运行中旧请求不被破坏 |
| 远端 PostgreSQL 不可达 | `local_only` 无影响；兼容导出模式仅暂停用户主动导出，本地事务和推理继续 |
| 远端恢复 | 兼容导出按用户确认重试；不得把远端数据回写覆盖本地事实 |
| 本地 SQLite 不可写 | 控制面进入只读故障，禁止假成功 |
| 主节点重启 | 从 SQLite 恢复下载/部署状态，重新探测 Worker，不从进程内状态猜测 |
| Tailscale 地址改变 | profile 保留 cluster_id，重新验证 endpoint；不得创建重复集群 |
| TOTP 时钟漂移 | 只允许有限时间步窗口并记录 drift；超出窗口要求用户修正主节点时间，不扩大长期接受范围 |
| Auth App 连续失败/恢复码重放 | 限速并临时锁定；成功消费的恢复码立即失效，管理员操作也需本地审计 |
| Tailscale 换网授权失败 | 保留旧 active binding，pending 状态过期自动清理，不创建半绑定用户 |
| 两个主节点同时出现 | cluster epoch/fencing 拒绝旧主写入和旧激活结果 |

---

## 14. 安全、许可证与供应链

- 默认源清单由开发团队提供并随发布版本内置（HTTPS Hugging Face/ModelScope 官方端点，见 §7.1）；用户更换源时必须显式配置并确认，仅允许 HTTPS 官方端点与用户明确配置的自定义源/私有镜像。
- 禁止任意 HTTP 重定向到本机、链路本地地址或受保护网段；主节点模型分发端点是单独的受信路径。
- 所有相对路径经过 traversal 检查；压缩包导入防止 Zip Slip、符号链接逃逸和超额展开。
- `trust_remote_code` 默认关闭；需要时必须是 curated、固定 revision、固定摘要和隔离运行。
- 下载 token、数据库密码和集群密钥不进入日志、manifest、SQLite/PostgreSQL 业务表或前端状态响应。
- Auth App/TOTP 密钥只在用户主节点生成和保存；字符串/二维码 provisioning 只显示一次，页面、日志、遥测和导出诊断不得留存原文。
- 默认 TOTP 使用随机独立密钥、30 秒时间步和有限漂移窗口；登录失败限速、连续失败锁定、恢复码一次性消费、换绑/撤销和管理员操作写入本地审计。
- 用户丢失 Auth App 与恢复码时，开发组没有远程重置能力；只能由用户本人通过主节点离线恢复/备份流程恢复或重建账户。
- Tailscale `tailnet_id`/`tailscale_user_id` 只保存最小绑定元数据；不收集 Tailscale 密码，不把用户的第三方 IdP token 或 OAuth secret 上传开发组。
- 第三方协议门：默认流程只依赖 Tailscale 官方客户端/网页、RFC 6238 和外部 Aegis，不新增 GitHub OAuth 登录；若集中注册 Tailscale OAuth app、白标/再分发 Tailscale 能力或引入 GitHub OAuth，必须先完成条款、scope、隐私和合作方审批，未通过前保持 `Planned`。
- 模型卡许可证、gated 状态和用户接受时间写入审计元数据；这不替代用户遵守模型许可证。
- 主节点向 Worker 分发时使用现有集群认证并增加请求签名、时间窗、artifact digest 和 Range 响应校验。
- 删除模型采用引用计数与延迟 GC；正在推理、回滚保留或任务 journal 引用的工件不可删除。

---

## 15. 可观测性与界面

模型页至少展示：

- 模型别名、来源、resolved revision、artifact digest 短码。
- 格式、模型族、参数量、量化、上下文、许可证。
- 下载总量/已完成/速度/ETA、重试次数和失败阶段。
- 本机/各节点兼容结论、占用磁盘、加载状态和最近健康时间。
- 目标节点、已准备节点、实际参与节点、fallback 原因。
- 当前 active、上一 rollback、部署 epoch。

集群页至少展示：

- 当前 profile、cluster_id、主节点 canonical endpoint。
- Tailscale IP/MagicDNS、TCP/API 健康和最近验证时间。
- 本地 SQLite 状态、artifact store 容量、用户备份状态；若启用兼容出口，再显示导出状态，不显示“远端数据库健康”作为主节点健康。
- “编辑主节点地址”“测试连接”“切换集群”“导出脱敏诊断”命令。

禁止用单一绿色“已下载”覆盖 `downloaded / verified / loadable / deployed / active` 五种不同状态。

---

## 16. 分期实施计划

### 16.0 2026-08-08 启动批次：无硬件并行路线

本批次不等待 Android、从节点或多 PC 设备，先把控制面、模型工件和可恢复任务的正确性收口。每个阶段都必须产出可审计的 fixture、日志或状态转移证据；没有真实设备的部分使用本地故障注入和模拟节点，不得把模拟结果写成跨节点部署完成。

| 批次 | 目标 | 当前起点 | 本批次完成判据 | 依赖/明确不做 |
|---|---|---|---|---|
| **M0.1 契约收口** | Schema 版本、状态枚举、迁移映射和 fetcher 事件成为唯一接口 | `Completed`（25/25 Python + 25/25 TS） | 兼容清单、未知字段策略、时序 fixture 和版本提升规则已机器可读并双端一致 | 独立 fetcher venv/打包仍是 M0 环境遗留项 |
| **M1.1 本地事实源收口** | SQLite 重启、只读故障和现有远端兼容路径可证明 | `Completed（兼容基线）` | 事务回滚、重启保留 registry/job/profile、现有 PG 兼容路径的离线积压与恢复补投均有 `control` 用例 | `M1.2` 已完成；当前进入 `M1.3` |
| **M1.2 主节点 SQLite 资产自持** | 让用户在没有 PostgreSQL 时完整运行并掌握所有资产 | `Completed（存储实现与双平台隔离环境门，2026-08-09）` | local-only、SQLite 核心域、v4 manifest/引用完整性、`.qlhmigrate` 流式资产包、SQLite v5 认证资产和 Python 主运行路径均完成；Windows 11 与 WSL2 Ubuntu 22.04 已分别通过干净临时目录、损坏活动库和真实只读 ACL/权限门，Python/Node 共库保持 schema v5，失败均 fail-closed。Python `1720 passed / 7 skipped`、control-svc `297/297`、gateway `104/104` | 下一票执行 `M1.3`；完整安装、首次认证、模型导入/任务恢复组合冒烟留给后续联合发布门 |
| **M1.3 远端 PostgreSQL 退场准备** | 将远端库降为可删除的用户主动兼容出口 | `Completed（2026-08-10）` | SQLite v6 退场状态、加密备份/恢复演练、脱敏 manifest、哈希锁定、outbox/projector/config/env 清理和 verify CLI 均通过；Python/Node 生产图与安装包无远端连接/驱动 | 旧兼容源文件仅供迁移审计；下一票 `MF-AUTH-N1` |
| **M2.1 工件导入收口** | 既有模型目录进入内容寻址仓库，失败隔离且源文件不变 | `Completed`（实际 catalog 迁移证据已登记） | 8 个条目/10 个预期来源实跑；2 个模型 ID 的 Safetensors+GGUF 共 4 个工件、46 个 blob 成功，6 个条目 `missing`；源文件不变 | Qwen 1.8B 保持 inspection only |
| **M2.2 运行时准入侧车** | 权重加载与 control-svc 隔离，终态成为节点/profile 运行时事实 | `Completed`（基础与本机门） | DeepSeek 7B GGUF 与 Qwen2.5 0.5B Safetensors 均已真实加载 `ready`；超资源模型仍安全 `resource_rejected`；崩溃/超时/错协议不影响主进程，SQLite v2 登记终态 | 更大 Safetensors/量化 profile 与真实 Worker 逐模型准入 |
| **M2.3 运行时准入接线** | import/pull/API/部署过滤共用节点/profile 运行时事实 | `Completed`（本地控制面门） | query/retry/invalidate/import API、pull adapting、稳定指纹、`stale`、prepare/activate 双重过滤完成；DeepSeek GGUF API retry→prepare=`ready` | 真实 scheduler/Worker 心跳接线仍属 M5；本地管理 UI 已进入 M2.4 |
| **M2.4 本地模型管理控制面** | 工件、运行时、导入、拉取任务与用户代理进入 Web 设置工作区 | `Completed`（本地 mock 门，2026-08-09） | `GET /models/artifacts` 汇总 alias/存储/runtime；`GET /models/pull` 任务列表；网关精确透传；工件导入、复检、失效、任务取消、代理保存/清除均可操作；前端单元 `27/27`、浏览器 mock `1/1` | 真实权重/Safetensors 由 M3.2 验收；跨节点分发仍待验收 |
| **M2.5 来源/凭据与拉取预检 UI** | 来源、凭据状态、许可接受和 resolve 确认进入 Web 设置工作区 | `Completed`（本地 mock + loopback 门，2026-08-09） | 凭据列表仅返回 ref/保护器/时间；gateway 精确透传 resolve/credentials/licenses；来源编辑/禁用/恢复、DPAPI 凭据只写/删除、许可接受/撤销完成；拉取携带 `source_id` 且旧预检随表单变化失效 | 真实 gated 账号与非 Windows credential adapter |
| **M3.1 一键 pull 收口** | pull 可解析、可取消、可恢复、可观测 | `Completed`（Windows 安全门 + 小工件真实网络门） | Windows DPAPI、Bearer、代理优先级、gated 双重门与 loopback API 已验证；7897 真实 CONNECT/resolve/完整下载/16 KiB 中断/206 续传/SHA 一致通过 | 真实权重由 M3.2 验收；重启/掉线由 M3.3 验收；gated 和非 Windows adapter 仍待验收 |
| **M3.2 真实权重与 Safetensors 准入** | 公开完整仓库经生产 pull 进入可运行工件 | `Completed`（单机真实网络 + CUDA 门，2026-08-09） | Qwen2.5 0.5B 固定 revision 的 10 文件/999,604,126 bytes 完整拉取与 SHA 通过；工件 `f8dcc256…bbe9c` 两次 CUDA BF16 加载 `ready`，本地 prepare=`ready`；当时 control 267/267 | M3.3 已完成；gated 账号仍为外部凭据门 |
| **M3.3 进程重启与代理掉线恢复** | 同一 SQLite/staging job 跨真实 control 生命周期续跑；瞬时传输故障有限重试 | `Completed`（真进程 + 可控代理 socket 门，2026-08-09） | 4 MiB 固定 GGUF 在 393,216 bytes 强杀后由第二进程重新 resolve 并从同偏移 206 续传；代理在 196,608 bytes 断开并短时 503，同一 job 第 3 次尝试恢复；最终 manifest SHA/size 一致，永久不可用为 `transfer_unavailable`；control 274/274 | 真实 gated 账号、认证代理、非 Windows credential adapter 仍为环境门；不依赖 7897 常驻 |
| **MF-MEM-N0 显存分页能力门** | 判断 8 GB NVIDIA GPU 是否应采用 UVM/虚拟显存 | `Completed`（文档与本机诊断门，2026-08-09） | WDDM、`concurrentManagedAccess=0`、GPU prefetch/advise 拒绝和 pinned 传输下界均已实测；Windows/WSL UVM `No-Go`、显式 pager `Go`、Linux UVM `Conditional Go` | `MF-MEM-N1` 已完成；进入隔离 `explicit_layer_pager` 前不修改生产 runtime |
| **MF-MEM-N1 transport harness** | 固定 tile 对照 pageable/pinned、单/双/三槽和计算重叠 | `Completed`（Windows 真实 CUDA transport 门，2026-08-09） | 64/256/1024 MiB × 4 模式两轮均 12/12；pinned raw H2D/D2H 明显优于 pageable；双/三槽收益不足且 P95 stall 放大；checksum/峰值资源均记录 | `MF-MEM-N2` 合成 sidecar 已完成；三槽冻结，真实层计算进入 N2R |
| **MF-MEM-N2 explicit layer pager** | 固定 64 MiB、双槽、有界 host 预取、显式 event、取消与回收 | `Completed`（合成 sidecar gate，2026-08-09） | N2 计划 4/4；CUDA batch=1/4 输出 digest 一致，取消第 2 层后回收 allocated/reserved=0，CPU equality 通过；WDDM overlap=0%，无吞吐收益结论 | `MF-MEM-N2R` 真实 NF4 基线已完成；固定 tile bridge 进入 N2F |
| **MF-MEM-N2R 真实 NF4 对照** | 固定 7B Safetensors、resident/auto、模块级显式迁移、CPU-only 安全门 | `Completed`（真实基线与适配器门，2026-08-09） | artifact gate 3/3、真实对照 6/6；context=128 resident/auto digest 一致；28 层显式 page-in/out；CPU-only 明确资源拒绝 | 固定 tile/延迟决策已由 N2F/N2P 隔离验证；不接生产 runtime |
| **MF-MEM-N2F 真实固定 tile/取消/长输出门** | 64 MiB × 2 bridge、显式分块搬运、layer-boundary cancellation、ctx8192 边界及 output 32/128 | `Completed`（真实固定 tile/取消/长输出门，2026-08-09） | N2F 计划 3/3；ctx128 完整、ctx128/8192 取消、output 32/128 完成；bridge 与模块驻留分开记录，专项回归 9/9 | 只证明受控搬运和回收，不证明吞吐优化；N2P 已完成并冻结默认 host prefetch |
| **MF-MEM-N2P 真实预取/重叠决策** | 预取距离、stream overlap、TTFT/inter-token 延迟和 resident/auto 对照 | `Completed（Windows 真实延迟/预取决策，2026-08-09）` | N2P 计划 3/3；digest 一致；host staging overlap 100% 但 CUDA H2D overlap 0%；E2E/TTFT 未改善，inter-token P95 仅小幅改善；取消回收通过 | `Freeze` 双槽 host prefetch overlap 默认优化；仅原生 Linux + 32 GB RAM 条件满足时进入 N3 |
| **M4.1 多集群档案收口** | 档案、验证、端点和 cluster_id 防串线语义稳定 | `Completed`（控制面本地门） | schema-shaped profile DTO、activate/current、删除清理、active/env/Tailscale 候选优先级和两个 cluster 隔离已验证 | 客户端 UI、端点编辑和真实 Tailscale 重连仍未验收 |
| **MF-AUTH-N0 认证边界调研** | 冻结 Auth App/TOTP、主节点数据所有权、Tailscale 绑定和第三方协议边界 | `Completed（文档调研，2026-08-09）` | RFC 6238、`otpauth://`、Aegis GPL-3.0、Tailscale 非 IdP、OAuth clients 与 alpha OAuth Apps 的差异已写入 §4/§10/§14/§20；明确不把 Tailscale 当 QLH 通用登录 | 无代码实现；下一票 `MF-AUTH-N1` |
| **MF-AUTH-N1 本地 Auth App 用户管理** | 主节点本地 owner/admin/member、TOTP 字符串/二维码注册、恢复码、登录/撤销和本地审计、业务 API 授权与用户管理 UI | `Validating（N1W/N1A 开发门完成，2026-08-10）` | SQLite schema v7、Windows DPAPI/Linux Secret Service adapter、owner bootstrap、TOTP 双下发/确认、恢复码、Bearer 会话、失败锁定、用户 CRUD、会话列表/撤销、gateway 四级授权矩阵和账户工作区已通过；control `313/313`、gateway `113/113`、前端 `29/29`、浏览器 `5/5` | 下一票转 `MF-AUTH-N2` 客户端绑定/换网 UI 与授权流程；默认单体切换、Linux Secret Service/DBus、完整安装首登与凭据恢复集中到最终跨平台联合发布门；不接 Tailscale OAuth |
| **MF-AUTH-N2 一一 Tailscale 绑定与换网** | 绑定 `tailnet_id + tailscale_user_id + node_id`，支持已有账号授权、首次注册引导和换 tailnet | `Validating（N2L/客户端开发门完成，2026-08-10）` | N2 定向契约 `7/7`、control `317/317`；本人/owner/admin 权限、只读本机状态安全投影、pending→active 与旧 active→revoked 原子切换、跨用户误绑定拒绝、撤销幂等、脱敏和账户工作区接线已通过；前端 `30/30`、浏览器 `5/5`；Windows 本机只读探测覆盖 IPv4/IPv6；OAuth Apps 保持禁用 | 仍需真实绑定/换网、旧节点网络侧撤销、Linux 和双机实网；不以 loginName/email 作主键 |
| **MF-AUTH-N3 可选 Tailscale OAuth Apps 适配器** | 仅为确有需要的用户 tailnet 提供一次性设备授权 | `Candidate（协议门未开）` | 每个用户 tailnet BYO app、最小 scope、单 tailnet/alpha 限制和无 refresh token 全部显式展示；Tailscale 条款/合作/批准评审通过后才可进入实验 | 默认不启用；未通过协议门保持冻结 |
| **M5.0 无硬件部署模拟器** | 在真实设备前验证 prepare/activate/partial/rollback 状态机 | `Completed`（模拟节点本地门） | 三节点能力矩阵、错 digest/离线拒绝、partial 激活、epoch 切换、重启读取和 rollback 已验证 | 真实 PC 分发、Worker 预热、性能和跨节点回滚不在本批次 |

**启动顺序**：先 M0.1 → M1.1/M2.1 并行，再优先执行 `M1.2`（SQLite 资产自持）和 `M1.3`（远端退场准备）；其后执行 `M3.1/M4.1` 与 `MF-AUTH-N0` 并行，再执行 `MF-AUTH-N1` → `MF-AUTH-N2`，最后才评估 `MF-AUTH-N3` 和 M5.0。认证域先在主节点闭环，不跨阶段复制第二套事实源，也不因 Tailscale OAuth alpha 而阻塞本地登录。M1.3、N1W、N1A、N2L 和 N2 客户端开发门已关闭，当前工作转为 N2 真实 Tailscale/跨平台联合验收；M5 真实部署控制器仍保留原 M5 的 L1 前置。

**本轮排期输出**：M3.3、`MF-MEM-N1`、`MF-MEM-N2` 合成 sidecar、`MF-MEM-N2R` 真实 7B NF4 基线、`MF-MEM-N2F` 固定 tile/取消/长输出门及 `MF-MEM-N2P` Windows 真实延迟决策已关闭；`MF-AUTH-N0` 已完成边界调研，`MF-AUTH-N1W`、`MF-AUTH-N1A`、N2L 本机只读状态适配和 N2 客户端绑定/换网 UI 已完成开发门。当前工作为 N2 真实 Tailscale CLI/浏览器授权、旧节点撤销和双机实网验收；旧 projector/outbox 只作为迁移审计，不是目标架构。Linux 真机暂缺不阻塞按契约开发，但 Secret Service/DBus、完整安装首登、纯 IPv6/混合双栈、真实 Tailscale 与双机撤销必须在设备到位后一次性联合验收，未验收前保持 `Validating`。N2P 的 host staging overlap 未转化为 CUDA H2D overlap 或端到端收益，因此双槽 host prefetch 默认优化冻结；UVM 仍仅在原生 Linux + 32 GB RAM 条件满足时进入 `MF-MEM-N3` 能力门。

**启动基线（2026-08-10）**：Python 全量 **1714 passed / 27 skipped**、control-svc **317/317 passed**、gateway **113/113 passed**；前端单元 **30/30**、浏览器 mock **5/5**，前后端构建通过，前端生产依赖审计 **0 vulnerabilities**。control SQLite schema v7 已包含 M1.3 退场状态及 N1 会话/锁定状态，加密备份/恢复演练、`storage:retire-postgres prepare|retire|verify`、Auth App、会话/角色授权、N2 绑定契约和 N2L 本机状态安全投影均通过。认证会话、绑定元数据和用户管理资产由用户主节点 SQLite/OS credential store 持有，TOTP 原文不入库。NET-N1 仍为本地双栈 `Validating`，不把双机实网/真实 Tailscale IPv6 写成已完成；当前 N1/N2 仍在验证，后续只推进真实环境联合验收，不提前开放 N3 或 Tailscale OAuth。

> 说明：本节启动顺序、排期输出和启动基线保留原始阶段记录；其中出现的“当前执行 N1/下一票 N2”以本页顶部 `MF-AUTH-N2 实施复核` 和表格状态为准。

#### MF-N1：M0.1 契约收口（首个执行票）

| 字段 | 内容 |
|---|---|
| 状态 | `Completed`（2026-08-08，本地门） |
| 目标 | 让 M1-M5 后续实现只依赖一套可演进契约，避免再次出现状态枚举、未知字段和版本迁移分叉 |
| 输入 | `schemas/*.schema.json`、`schemas/migration-map.json`、`schemas/README.md`、`fixtures/model-fleet/`、Python/TS validator |
| 文档交付 | schema 兼容规则（新增字段、删除字段、枚举扩展、版本提升）、fixture 命名/审查规则、fetcher 事件时序、迁移失败与回滚边界 |
| 后续实现交付 | 已增加兼容性清单、fetcher 合法/非法时序 fixture、终止态封闭、job_id 一致性和进度单调性验证；旧客户端/新字段策略已落到 `schemas/model-fleet-compatibility.json` |
| 完成判据 | Python/TS 对全部 fixture 结论一致；迁移映射自洽；任何不兼容变更必须提升 schema version；文档、schema 和测试在同一变更中更新 |
| 非目标 | 不下载真实模型、不引入第二套 registry、不改变推理引擎、不宣称 M1-M4 完成 |

MF-N1 已完成；现有 M1.1 SQLite/legacy projector 基线、MF-MEM-N0/N1、N2 合成 sidecar、N2R 真实 NF4 基线、N2F 固定 tile/取消/长输出门与 N2P Windows 真实延迟决策均已完成，`MF-AUTH-N0` 文档调研已完成，M3.3、M5.0 模拟器、M2 实际 catalog/runtime sidecar/admission、M2.4-M2.5 Web 控制面及 M3.1-M3.3 网络/运行时/恢复门均已完成。Qwen2.5 0.5B 已取得完整 pull、内容寻址、CUDA BF16 `ready` 和本地 prepare 证据；M3.3 已取得跨真实 control 生命周期与代理 socket 掉线恢复证据；N2P 已冻结双槽 host prefetch 默认优化，显式 pager 仅保留容量 profile。`M1.2` 的七片存储实现与 Windows/Linux 隔离环境故障门已完成，下一可执行票为 `M1.3` 远端退场准备，再进入 `MF-AUTH-N1`。显存票仍仅在原生 Linux + 32 GB RAM 条件满足时进入 `MF-MEM-N3` UVM 能力门；完整安装、远端 PG、Tailscale、Auth App 实现和 PC Worker 继续保留环境/实现门。

#### MF-AUTH-N0：Auth App、用户所有权与 Tailscale 绑定调研（文档完成）

| 字段 | 决策 |
|---|---|
| 状态 | `Completed（文档调研，2026-08-09）` |
| 认证 | QLH 本地用户名 + Auth App/TOTP；无 email 绑定；首次注册同时下发 Base32/`otpauth://` 字符串和二维码 |
| 用户数据 | `local_users`、TOTP 密钥引用、恢复码哈希、绑定和审计只在用户主节点；不投影开发组服务或远端 PostgreSQL |
| Tailscale 语义 | 绑定稳定 `tailnet_id + tailscale_user_id + node_id`，不把 Tailscale 当 QLH IdP；已有账号走官方登录/授权，没有账号走官方注册引导 |
| OAuth 决策 | OAuth clients 仅 API client credentials；OAuth Apps device provisioning 当前 alpha/单 tailnet/一次性 token，降为 `MF-AUTH-N3` 可选实验，默认不注册中心 app |
| Aegis/许可证 | 推荐用户外部安装 Aegis；其仓库为 GPL-3.0，QLH 不内嵌代码。若项目组开发自己的 Auth App，另设 vault/签名/审计门 |
| 下一步 | `MF-AUTH-N1`：本地用户管理和 Auth App 注册/登录/恢复；完成前不接 Tailscale OAuth |

### M0：冻结契约与事实源

前置：微服务清理门未完成时只做设计和测试桩。

任务：

1. 冻结 artifact manifest、pull job、deployment 和 cluster profile schema。
2. 确定 control-svc 为模型注册和控制数据库唯一写者。
3. 列出 Python 内置注册表、PostgreSQL `cluster_config`、TS JSON 的一次性迁移映射。
4. 定义模型能力枚举，禁止用 `model_type` 推导所有分布式能力。
5. 冻结 `model-fetcher` 的独立工具环境、结构化进度协议和版本锁；不得复用项目全局解释器。
6. 以实际 Windows/Linux 发布方式选择 SQLite 驱动，先解决 Node 运行时/原生模块打包，再开始 M1。

**进度（2026-08-09 复核）**：

- [x] 任务 1：六个 schema 落地 `schemas/`（artifact-manifest / pull-job / deployment / cluster-profile / migration-map / fetcher-progress，draft-07），另有机器可读兼容清单 `schemas/model-fleet-compatibility.json`；fixtures `fixtures/model-fleet/` 当前 8 个合法 + 10 个非法/时序样例；Python/TS 两侧 schema 各 `25/25`（jsonschema / ajv + ajv-formats），并对 fetcher 事件执行额外语义校验。
- [x] 任务 2：确认 control-svc 为模型注册唯一写者（`control/src/data/model-registry-store.ts` 已承载用户模型 CRUD；迁移后由 SQLite 取代 JSON，其他服务只读 API）。
- [x] 任务 3：迁移映射清单 `schemas/migration-map.json`（三处旧事实源：`model_config.py:BUILTIN_MODELS`、control-svc `model_registry.json`、PostgreSQL `cluster_config` → 目标表与字段映射、幂等键、去重规则）；自洽性由双语言测试锁定（目标表已登记、source_id 唯一、不产生重复 artifact）。
- [x] 任务 4：能力枚举冻结（`full_worker` / `pytorch_layer_pipeline` / `llama_cpp` / `task_stage`，`additionalProperties:false` fail-closed）；说明见 `schemas/README.md`。
- [x] 任务 5（协议部分）：model-fetcher 结构化进度协议 `schemas/fetcher-progress.schema.json`（started/progress/resolved/verified/failed/completed 事件，单向 stdout JSON 行，control-svc 唯一收件人）。
- [x] 任务 6（调研结论）：**SQLite 驱动选择 `node:sqlite`（Node ≥22.5 内置，`DatabaseSync`/`backup` 可用，本仓库 Node 24 实测通过）**——无需 better-sqlite3 原生编译，规避 Windows/Linux 打包原生模块问题；M1 直接使用。
- [ ] 任务 5（环境落地）/ 任务 6（打包实测）：model-fetcher 独立 venv（仅 huggingface_hub 等）与 wheelhouse；`node:sqlite` 在 PyInstaller/Electron 类发布形态的打包验证（M1 前置）。

验收：同一 fixture 经 Python/TS schema validator 得到一致结果；旧数据迁移可重复执行且不产生重复 artifact。

### M1：主节点本地事务数据库

任务：

1. 在 control-svc 引入 SQLite repository、迁移器、WAL、backup 与 health。
2. 先迁 cluster settings、model registry、cluster endpoints。
3. 兼容期可保留 outbox/PostgreSQL projector，但必须标记为 `legacy_remote_enabled`，不得成为默认路径。
4. 将 `/api/db/health` 兼容映射到本地 SQLite/可选导出状态，不能把远端健康当成主节点健康。
5. 将远端连接探测移出请求关键路径；最终目标是默认不探测、不连接 PostgreSQL。

**进度（2026-08-09 复核）**：

- [x] 任务 1：`control/src/data/sqlite-store.ts`——`node:sqlite`（M0 调研结论）本地库：WAL/foreign_keys/busy_timeout、`PRAGMA user_version` 事务化迁移器（v1 建 9 张目标表：cluster_settings/model_registry/cluster_endpoints/cluster_profiles/catalog_models/pull_jobs/artifacts/deployments/outbox；v2 增加节点/profile 维度的 `artifact_runtime_checks`）、在线 backup（模块级 backup API）、健康探测（SELECT 1 + 可写性临时事务，只读故障明确报错）。v1→v2 原位升级保留旧数据；惰性打开，测试与运行不污染 cwd。
- [x] 任务 2（repository + 迁移执行器 + 读路径切换）：三个 repository（`cluster-settings-repository.ts` 幂等 upsert / `model-registry-repository.ts` model_id 主键 + sha256 摘要去重（NULL 摘要不参与去重，正式摘要由 M2 inspector 补齐）+ payload JSON 保存完整注册条目 / `cluster-endpoints-repository.ts` cluster_id 保留不重复建群）+ `legacy-migration.ts` 迁移执行器（catalog seed、旧 model_registry.json、cluster_config loader 三源；幂等可重复、同摘要只注册一次、无摘要以 NULL 入库、pg 不可用跳过不阻塞）。**读路径切换完成**：`models.controller.ts` 的 registry CRUD（GET/POST/DELETE /models/registry）改用 SQLite repository，契约零变化（models-contract 15 用例适配 SQLite override 后全绿）；`settings.controller.ts` 的 `/user/settings` 始终由 SQLite 本地事实源接管读写，local-only 不探测/投影远端，显式兼容出口才附带 outbox；`bootstrap.controller.ts` first-connect 成功时把主节点 endpoint 登记到 SQLite（cluster_id 保留不重复，兼容旧 v2 文件无唯一索引）。旧 `ModelRegistryStore` 保留为迁移源/兼容。Python 侧 `scripts/export_model_catalog.py` 导出 `BUILTIN_MODELS` → `build/model-fleet/catalog-seed.json`（字段对齐 migration-map field_map，实测 8 条）。
- [x] 任务 3（兼容基线，非目标架构）：`outbox.service.ts` + `postgres-projector.ts` 已实现后台退避、幂等投影和未配置时跳过；该路径现标记为 `legacy_remote_enabled`，后续由 M1.3 退场。
- [x] 任务 4（兼容基线）：`GET /storage/health` 已能同时表达 local sqlite 与旧 remote 状态；后续 M1.2 将默认结构收敛为 `local_only`/`legacy_export_*`，`/db/health` 仅保留兼容映射。
- [x] 任务 5（兼容基线）：远端探测已移出请求关键路径；M1.3 前不得把 projector 重新接入登录、调度或资产读取关键路径。
- [x] 收尾（本地门）：`control/test/model-fleet-recovery.e2e-spec.ts` 覆盖事务回滚、重启保留 registry/job/profile、PG 离线积压/恢复补投和 stale aggregate version 拒绝；远端 PG 断开 24h 与生产耐久性仍待真实环境。

测试：`control/test/sqlite-store.e2e-spec.ts` + `control/test/storage-health.e2e-spec.ts` + `control/test/legacy-migration.e2e-spec.ts` 及 `control/test/model-fleet-recovery.e2e-spec.ts`；含 v1→v2 保留数据门，control-svc 当前全量 **274/274 passed**。这些证据证明兼容基线，不证明远端 PostgreSQL 是长期依赖。

验收（M1.1 兼容基线）：远端 PostgreSQL 连续断开 24 小时，主节点重启后配置、模型注册和 cluster profile 不丢；旧远端恢复后可幂等追平且无重复记录。最终 local-only 验收由 M1.2/M1.3 负责。

### M1.2：主节点 SQLite 资产自持（存储实现与双平台隔离环境门完成）

前置：M1.1 本地事务基线已完成；本阶段只落地主节点存储和备份，不接 Tailscale OAuth，不等待 PC Worker。

当前已实施首片（2026-08-09）：`ConfigDao` 默认 `local_only`；`/user/settings` 始终写入主节点 SQLite，旧 PostgreSQL 仅在显式开启时作为兼容导出；local-only 不启动后台 projector；`SqliteStore` 提供 scrypt + AES-256-GCM 用户加密备份、quick-check、口令校验、恢复前验证和失败回滚；`storage/health` 显示 `disabled/legacy_export` 与本地导出状态；旧 v2 SQLite 文件的 endpoint upsert 兼容无唯一索引。控制面新增 `storage:backup` CLI，支持 `backup`、`verify`、`restore`。

第二片（2026-08-09）已完成：新增 SQLite v3 的 `sessions`、`session_messages`、`workflow_journal`、`review_tickets` 表；control-svc 默认将会话/消息、工作流 journal、审查工单写入同一主节点数据库；旧 JSON 文件首次启动只读导入且保留原文件，显式传入文件仍可用于兼容回滚。新增 `control/test/sqlite-core-stores.e2e-spec.ts`，验证跨实例重启、旧 JSON 导入和三域持久化。

第三片（2026-08-09）已完成：SQLite v4 新增 `artifact_manifests`，登记 alias、artifact digest、文件摘要集合、manifest payload 和引用元数据；生产写入在落盘前 fail-closed 校验引用字段、相对路径、逐文件摘要、聚合 artifact digest 和 blob 存在性。启动时先以 SQLite payload 修复缺失/损坏的 manifest 文件，再将文件系统现有 manifest 幂等回填 SQLite；只有外部 blob 已存在且校验通过才允许反向重建。加密 `backup/verify/restore` 的 dry-run 现同时检查 manifest payload 摘要、deployment/runtime 引用和可选模型目录中的外部 blob；`storage:backup` 新增 `--model-store`。SQLite 备份不包含 blob 字节，迁移新主节点时仍须随附用户自己的模型目录。control-svc 全量 `283/283`、gateway `104/104`。

第四片（2026-08-09）已完成：新增版本化 `.qlhmigrate` 本地资产包，以固定大小分块流式执行 scrypt + AES-256-GCM，不使用内存聚合 ZIP；包头经 AAD 认证，逐条记录 SQLite 加密快照和 SQLite manifest 实际引用的去重 blob 的大小/SHA256。导出从已生成的 SQLite 快照读取 manifest，写入期间再次计算源摘要；verify/restore 先在 staging 解密、复核包长度、认证标签、逐条摘要、manifest 数量、blob 大小及 deployment/runtime 引用，再重建 manifest。恢复前检查模型/SQLite 目录可写性和空间；模型目录与 SQLite 分别保留 `pre-restore` 回滚副本，任一阶段失败恢复旧资产。自动门覆盖本机全新目录恢复、包篡改、来源缺 blob、空间不足、不可创建目标、SQLite 阶段注入失败，以及 SQLite 已切换后 reindex 失败的双资产回滚；staging、quarantine 和无引用 blob 不进入包。control-svc 全量 `290/290`、gateway `104/104`。

第五片（2026-08-09）已完成：SQLite v5 新增 `local_users`、`user_authenticators`、`auth_recovery_codes`、`tailscale_bindings` 和 `auth_audit_events`。本地用户名大小写归一唯一，非 revoked owner 单例；TOTP 只保存规范化 `os:` 引用，允许一个 active 与一个 pending 并在确认时原子撤销旧 active；恢复码只接受编码后的 scrypt/Argon2id 哈希并支持批次替换/一次性消费；审计拒绝 secret/token/recovery code/`otpauth://`/Bearer/`tskey-`；Tailscale 绑定限制每用户一个 active/一个 pending、每个外部身份一个 active，冲突时事务回滚并保留旧 active。认证资产不写 outbox、不投影 PostgreSQL。新增 `control/test/auth-assets.e2e-spec.ts`，覆盖重启、乐观版本、明文禁入、换网失败回滚和 `.qlhmigrate` 新目录恢复；control-svc 全量 `295/295`。迁移包只携带 SQLite 中的 `secret_ref`，不携带 DPAPI/系统凭据密文，因此新主节点恢复后仍需后续凭据迁移/重新注册设计，当前不开放登录 API、不生成 TOTP、不调用 Tailscale。

第六片（2026-08-09）已完成：`src/local_store.py` 从 JSON 降级层改为共享 WAL SQLite，默认路径与 control-svc 统一到用户可写 `STATE_DIR/qlh-control.sqlite3`，并兼容继续使用尚未迁移的 cwd 旧库；Python 只创建兼容表，不修改 `PRAGMA user_version`。旧会话 JSON 首次初始化只读导入且保留原文件，异常计数不会阻断迁移。Python 单体和独立 inference-svc 启动均执行真实回滚写探针，不可写时 fail-closed；用户设置、会话 CRUD、对话读取、自动标题、推理完成提交、同步状态和 `/api/db/health` 均改为 SQLite 本地优先，显式 PostgreSQL 只做尽力兼容导出。推理持久化重复分支已在单体与 `EngineHost` 内分别收敛，测试隔离禁止写入用户真实数据库。验证为 Python `1720 passed / 7 skipped`、control-svc `297/297`、gateway `104/104`，control 构建通过。

第七片（2026-08-09）已完成：新增 `control/src/storage-environment-probe.ts` 与 `scripts/model_fleet_storage_environment_gate.py`，用脚本创建的隔离临时目录执行 Python→Node→Python 共库和 schema v5 交叉验证。Windows 11/Python 3.12.10/Node 24.16.0 已通过干净目录、破坏活动库头和真实 NTFS 写入 deny ACL，损坏库均报 `file is not a database`，只读目录均拒绝打开；WSL2 Ubuntu 22.04/Python 3.10.12 使用临时原生 Node 24.16.0，在 ext4 上通过干净目录、损坏库和目录 `0555`/文件 `0444`，只读写探针均报 `attempt to write a readonly database`。`SqliteStore.health()` 同步修正为写探针失败返回 `unavailable`。Windows ACL 已撤销，WSL 临时 Node 与所有探针目录已删除，脚本未打开用户状态库且未使用 7897 代理。回归为 control-svc `297/297`、control 构建通过、Python 存储/降级/inference 定向 `116/116`；Python 产品运行时代码未再变化，因此全量基线保持 `1720 passed / 7 skipped`。

任务：

1. 将认证、cluster/profile、model catalog/artifact 引用、pull/deployment、task journal、会话/设置、审查、审计和备份索引全部登记到 SQLite 迁移清单；明确每个域的用户资产所有权和恢复顺序。会话、task journal、审查已在 v3 登记，artifact manifest 已在 v4 登记，认证资产已在 v5 登记；Python 会话/设置主运行路径已复用同一 schema。本阶段完成。
2. 将默认启动配置固定为 `local_only`：无 `DATABASE_URL`、无 PostgreSQL 驱动、无远端网络时，Python 单体与 inference-svc 已可通过 SQLite 启动和保存本地推理历史；Windows/Linux 隔离运行时的干净目录与不可写 fail-closed 已通过。首次登录仍依赖后续 `MF-AUTH-N1`，完整安装、模型导入、部署与重启组合冒烟留在联合发布门。
3. 实现用户主动的加密 backup/export/import：schema/version、artifact manifest、deployment/runtime 引用完整性、外部 blob、认证元数据/密钥引用、流式 `.qlhmigrate`、本机全新目录 dry-run/恢复和双资产失败回滚已完成；仍需 OS credential store 的迁移/重新注册决策和完整新主节点发布验证，绝不导出明文 TOTP/令牌。
4. 将 `storage/health`、设置页和诊断改为本地 SQLite/用户备份为主；control-svc 与 Python `/api/db/health` 已将本地 SQLite 作为顶层健康，远端只显示可选 `legacy_export`，不再决定主节点健康。
5. 为升级、磁盘不足、SQLite 损坏、只读目录和主节点迁移建立故障注入矩阵；迁移包空间不足、篡改/缺失、不可创建目标和 SQLite 阶段失败回滚已自动化，活动数据库损坏、Windows NTFS deny ACL 与 Linux `0555/0444` 权限门已通过；任何失败都 fail-closed，不回退到无提示内存状态。物理安装器与整机组合冒烟不由本阶段单独关闭。

M1.2 完成判据（已通过）：

- local-only 下 Python/Node 使用同一用户可写 SQLite，核心资产、认证存储、备份/迁移包与 Python 主运行路径不依赖 PostgreSQL。
- Windows 与 Linux 隔离运行时各通过干净目录、活动库损坏和真实只读权限门，失败均 fail-closed，且不污染用户状态库。
- 备份/迁移在本机全新目录完成恢复、引用验证和双资产失败回滚；OS credential 密文不进入包。

后续联合发布门（不计入 M1.2 完成状态）：

- Windows/Linux 完整安装包各完成一次启动；`MF-AUTH-N1` 完成后再验收首次认证。
- 模型导入、任务恢复、部署和本地推理在完整安装环境组合冒烟。
- 用户资产清单、上游模型许可证边界、加密备份责任和“开发组无远程 reset”在 UI/文档中一致。
- `M1.3` 验证远端兼容出口关闭后核心 API、Worker bootstrap、调度和 UI 不创建远端数据库连接，并明确远端数据库可由用户删除。

### M1.3：远端 PostgreSQL 退场准备

M1.3 不是把用户数据迁移到另一个开发组服务，而是让旧远端路径成为可删除的兼容组件：

**实施结果（2026-08-10，Completed）**：SQLite v6 增加 `storage_retirement` 单行状态；`storage:retire-postgres` 在 `prepare` 阶段生成加密 SQLite 备份、执行恢复演练并生成不含秘密值的版本化 manifest，`retire` 仅在备份/manifest 哈希一致时删除 legacy outbox/index、清理配置和 `.env`，`verify` 再次检查状态、哈希和零远端配置。Python 主运行时、control-svc production graph、package requirements/spec/launcher 均不再连接或携带 PostgreSQL；`src/db.py`、历史 projector/outbox 仅作迁移审计源。全量回归为 Python `1709/1709`（27 skipped）、control `300/300`、gateway `104/104`。

1. 远端导出使用显式开关、用户自有连接信息和版本化 manifest；禁止远端回写主节点。
2. 新安装默认不携带或不启用 projector/outbox 运行时；升级包可以在用户确认后提供一次性迁移工具。
3. 迁移工具先做本地加密备份和恢复校验，再清理远端配置、凭据引用、projector 和 outbox 表；清理失败不影响 local-only 运行。
4. 发布门增加“拔掉远端数据库仍可完整运行”和“删除远端配置后资产可恢复”两项硬测试。

完成判据（已满足）：用户可以在不联系开发组、不提供远端数据库密码的情况下，将现有主节点切换为 `local_only`，验证备份和恢复后删除远端依赖；旧远端数据库即使永久消失，也不丢失用户本地资产。下一阶段转入 `MF-AUTH-N1`，实现真正的本地登录 API，不把 SQLite v6 存储完成误写成认证已开放。

### M2：内容寻址工件库与本地导入

任务：

1. 实现 staging、blob、manifest、引用和 quarantine。
2. 迁移现有 `models/`，只读取不破坏用户原文件；生成导入报告。
3. 实现 GGUF 与 Safetensors 静态 inspector。
4. 接入 inference-svc 隔离试加载。

**进度（2026-08-08 复核）**：

- [x] 任务 1：`control/src/data/artifact-store.ts`——`<QLH_MODEL_STORE>`（env 覆盖，默认 cwd/model-store）布局 staging/<job>/blobs/sha256/<2>/<digest>/manifests/<ns>/<name>/<tag>.json/quarantine/<job>；staging 只写隔离、同文件系统原子 rename 提交、相同 sha256 去重（重复提交只增引用）、quarantine 失败留痕（reason.txt）、manifest 引用计数驱动 GC（保留集 = manifest 引用 + 显式 extraRetained）、staging 路径穿越拒绝。
- [x] 任务 2（本地导入主体）：`control/src/data/model-import-service.ts`——目录（Safetensors 仓库）或单 GGUF 文件导入：只读源 → staging 拷贝 → 静态检查 → 原子提交 → 聚合摘要 `sha256:<aggregate>` artifact_id → manifest（对齐 schemas/artifact-manifest，能力枚举 fail-closed）→ 导入报告（imported/deduped/failed/quarantined/warnings）；源文件绝不修改；错误摘要永不进入 registry。
- [x] 任务 3：`control/src/data/model-inspector.ts`——纯 Node 无依赖：GGUF 头解析（magic/version/KV：general.architecture、当前 llama.cpp `general.file_type` 完整映射、架构专属 `*.context_length`、tokenizer 标记；值类型含 array/string/数值全族）；Safetensors 目录解析（config.json model_type→family 映射 + tokenizer 存在性）；能力判定白名单（qwen2/llama/mistral/deepseek/gemma/phi3），未知架构 fail-closed（仅 inspection，不自动部署）；`trust_remote_code` 恒 false。
- [x] 任务 2（批量入口）：`control/src/data/model-batch-import.ts` + `control/src/model-fleet-import.ts`，`npm run model-fleet:import -- --source <models-dir>` 扫描目录、输出原子 JSON 汇总、失败隔离、摘要去重；MF-N3 本地 fixture 已验证。
- [x] 任务 2 真实证据：对 `catalog-seed.json` 的 8 个内置条目实际运行迁移；10 个预期来源中 4 个成功、6 个源路径缺失，最终失败/隔离为 0。结果登记在本节与《总体下一步计划》变更记录中。
- [x] 任务 4（侧车基础与本机门）：`model_runtime_sidecar.py` + control 进程监管/CLI/SQLite v2 登记；DeepSeek 7B GGUF 逐文件 SHA256 后由 `llama-cpp-python/0.3.28` 真实加载为 `ready`（加载 2,720ms），Safetensors 因安全需求 17,595,895,384 bytes 大于本机可用组合余量 12,586,381,312 bytes 登记 `resource_rejected`，未触发 OOM。Qwen 1.8B 在 legacy adapter/信任策略完成前保持 inspection only。结果登记在本节与《总体下一步计划》变更记录中。
- [x] 任务 4（准入接线）：`runtime-admission.controller.ts` + `model-import-admission.service.ts` 提供状态 query/retry/invalidate 和 interactive import；pull executor 在 `adapting` 运行 sidecar 并保留非 ready 工件；runtime record 增加稳定指纹与 `stale`；部署模拟器在 prepare/activate 双重检查 node/profile/status/fingerprint。DeepSeek GGUF 经真实 API retry（2,660ms）后同指纹 prepare=`ready`。结果登记在本节与《总体下一步计划》变更记录中。
- [x] 任务 4（M3.2 Safetensors 真实准入）：固定 Qwen2.5 0.5B revision `7ae557604adf67be50417f59c2c2f167def9a775`，生产 pull 完成 10 文件/999,604,126 bytes 内容寻址工件 `sha256:f8dcc2568764cbd9551ae9b6250526170bb20b29ed8c562c9ce02bff143bbe9c`。Safetensors staging inspection 写入 `qwen2/full_worker/32768`；494,032,768 参数由 Transformers 4.47.1/Torch 2.13.0+cu126 在 CUDA BF16 两次离线加载 `ready`（2,534/1,674ms），独立 retry 与本地 deployment prepare 均为 `ready`。

- [x] 任务 4（M2.4 本地模型管理控制面）：`artifact-inventory.controller.ts` 汇总 manifest alias、文件大小、runtime check 和 runnable；pull controller 增加任务列表；gateway 对 `/api/models/artifacts|imports|runtime-checks|pull|network` 精确透传。`ModelFleetPanel` 在 Web 设置中提供工件、导入/拉取、任务和网络四个视图，支持导入并检查、复检、标记失效、取消任务和保存/清除用户级代理；前端单元 `27/27`、浏览器 mock `1/1`、control-svc `265/265`、gateway `103/103`。本票全程使用本地 fixture/mock，不使用代理。
- [x] 任务 5（M2.5 来源/凭据与拉取预检 UI）：`ModelCredentialStore.listStatuses()` 与 `GET /models/credentials` 只暴露 ref/exists/protection/updated_at；gateway 增加 `/api/models/resolve|credentials|licenses` 精确透传。`ModelFleetPanel` 新增来源与凭据视图，支持来源编辑/禁用/恢复、DPAPI 凭据只写/删除、许可接受/撤销；拉取请求显式携带 `source_id`，切源、改仓库/revision/pattern 后旧预检立即失效，只有 resolve=`ready` 才能确认。control-svc `265/265`、gateway `104/104`、前端 `27/27`、浏览器 mock `1/1`；本票未使用代理。

测试：工件/批量导入/sidecar/runtime admission 四套及跨 pull/security/deployment/schema 的准入相关专项均通过，control-svc 全量 **274/274 passed**；gateway **104/104**、前端单元 **27/27**、浏览器 mock **1/1**，生产依赖审计 0 vulnerability。GGUF 与 Safetensors 均已有真实 API 准入证据。

验收：相同文件去重；错误摘要永不进入 registry；主节点进程在试加载失败后仍健康；删除别名不误删共享 blob。

### M3：Hugging Face 一键 pull

任务：

1. 实现 resolve/dry-run、revision 锁定、allow patterns 和磁盘预算。
2. 实现下载任务、SSE、取消、重启恢复和代理配置。
3. 接入 OS credential store 与 gated model 许可确认。
4. 完成第一批 Qwen/DeepSeek/HF GGUF curated recipes。
5. 默认源与更换源：内置默认源清单（HF 官方/ModelScope/镜像回退）、源管理设置（endpoint/token/优先级/恢复出厂）、更换源后强制重新 resolve、manifest 记录实际来源。

**进度（2026-08-08 复核）**：

- [x] 任务 1（resolve 主体）：`hf-resolver.ts` + `model-http-client.ts` 使用 `undici 8.10.0` 调 HF API（`/api/models/{repo}?revision=`），返回完整 commit revision、文件清单、gated 与 license 元数据；`globMatch` 支持 allow_patterns，30s 超时。模型传输层支持每请求 Bearer header，并按 `QLH_HTTP_PROXY > user > direct` 动态选择 ProxyAgent，不触碰推理环境解释器和 `HF_HOME`。
- [x] 任务 2（主体）：`pull-job.service.ts`（状态机 queued→resolving→downloading→verifying→adapting→registered + failed/cancelled/rejected/quarantined/rolled_back，SQLite pull_jobs 持久化、幂等键去重、重启恢复 listActive、订阅者 SSE 推送）+ `pull-job-executor.ts`（resolve→流式下载/已有 SHA blob 硬链接复用→8 MiB 分块 SHA→GGUF 或 Safetensors staging inspection→原子提交→manifest→sidecar admission→registered；AbortController 同时取消下载和侧车）。`registered` 只表示工件保留，部署必须另查 `runtime_check`；`pull-job.controller.ts` 提供 POST/GET/DELETE + `GET /models/pull/{id}/events` SSE。断点续传（Range）在 `hf-downloader.ts`（fetch 流式 + append + 进度节流 + 大小校验）。
- [x] 任务 4：`curated-catalog.ts` 第一批 recipes（Qwen2.5-1.5B/7B GGUF、DeepSeek-R1-Distill-Qwen-7B GGUF，固定 revision + allow_patterns + 许可）。
- [x] 任务 1（本地门）：`POST /models/resolve` + `PullPreflightService` 完成 revision/file list/dry-run/磁盘不足拒绝；executor 在正式下载前复用同一预算检查。
- [x] 任务 5（控制面本地门）：`ModelSourceRepository` 内置 HF 官方/镜像/ModelScope 源清单；支持来源优先级、启停、恢复出厂、`source_id` 绑定和切源重新 resolve；只接受 `credential_ref`，明文 token/access_token fail-closed。
- [x] 任务 2（恢复门）：active pull 在 control-svc bootstrap 时 requeue 并重新 resolve，来源 endpoint 写入 job/manifest；`progress` 持久登记 `restart_count / transfer_attempt / transfer_retry_count / resumed_bytes / last_retry_error`，状态迁移不允许跳过 verifying/adapting 关键阶段。
- [x] 任务 3（Windows 本地安全门）：`model-credential-store.ts` 使用当前用户 DPAPI；`model-license-acceptance.ts` 持久化显式许可接受时间；preflight 与 executor 双重检查 credential/license；安全变更 API 仅 loopback；明文不进入 SQLite/job/manifest/响应。证据登记在本节与源码测试。
- [x] 任务 3（用户代理与公开 resolve）：`ModelProxyConfigRepository` 将用户代理保存到 SQLite；`PUT/DELETE /models/network/proxy` 仅允许 loopback，环境变量保持最高优先级且修改即时生效。`127.0.0.1:7897` 已通过真实 CONNECT，项目 HfResolver 已解析 `openai-community/gpt2@607a30d…`。
- [x] 任务 2/3（小工件真实网络门）：resolver 请求 `blobs=true` 并 fail-closed 校验 size/LFS SHA；严格验证 `206`/`Content-Range`；固定 `merges.txt` 经环境代理和 SQLite 用户代理完成全量下载、16 KiB 中断、续传和最终 SHA 一致。证据登记在本节与源码测试。
- [x] 任务 2/3（本地控制面 UI）：来源、凭据状态/写入/删除、许可接受/撤销和重新 resolve→确认拉取已进入 Web；明文 secret 不回显、不进入 source/job/manifest/SQLite。
- [x] 任务 2（M3.2 真实完整 pull）：`Qwen/Qwen2.5-0.5B-Instruct@7ae5576…` 经 7897 完成 10 文件/999,604,126 bytes 全量下载和逐文件 SHA；上游 LFS 权重摘要 `fdf756fa…b7fe` 一致。实跑修复整文件读入内存、Safetensors 能力未检查和已有 LFS blob 未复用；恢复 job 复用 988,097,824-byte 权重，后续预算只剩 11,506,302 bytes。
- [x] M3.3 真进程/代理故障门：`model-fleet-recovery-smoke.ts` 启动固定 4 MiB GGUF 源、可控 HTTP/CONNECT 代理和真实 `dist/main.js` 子进程。首个 control 在 staging 393,216 bytes 时被强杀；第二个进程使用同一 SQLite/model-store，bootstrap 重新 resolve 后发 `Range: bytes=393216-` 并注册。第二个 job 在 196,608 bytes 时被代理断开，代理随后短时返回一次 503，同一 job 第 3 次尝试从 196,608 bytes 恢复；两个 manifest 的 size/SHA 均与固定源一致。瞬时 fetch/stream/408/425/429/5xx 采用最多 4 次指数退避；连接/响应头超时在收到响应后解除，不截断已建立的慢速大文件流；Range/Content-Range 错位、摘要和磁盘错误不重试；耗尽后终态错误码为 `transfer_unavailable`。
- [x] M3.3 来源安全边界：自定义远程源继续强制 HTTPS；仅 `127.0.0.1`、`localhost`、`::1` 允许 HTTP，供本机镜像和确定性 smoke 使用，局域网/公网明文 HTTP 仍拒绝。M3.3 不依赖 7897 常驻。
- [x] MF-MEM-N1 固定 CUDA transport 门：`scripts/model_fleet_memory_transport.py` + `fixtures/experiment-plans/plan-model-fleet-memory-transport-v1.json` 在 Windows WDDM/RTX 4060 上完成 64/256/1024 MiB × pageable、pinned 单槽、双槽、三槽两轮 12/12；显式 checksum、H2D/D2H、pipeline overlap、stall P50/P95/max、pinned/VRAM 峰值全部记录。结论为 pinned raw transport `Go`，双槽仅条件进入 N2 sidecar，三槽冻结；未接生产 runtime。
- [x] MF-MEM-N2 合成 explicit layer pager 门：`scripts/model_fleet_explicit_layer_pager.py` + `fixtures/experiment-plans/plan-model-fleet-explicit-pager-v1.json` 在 Windows WDDM/RTX 4060 上完成 4/4；64 MiB 双槽有界 host 预取、显式 copy/compute event、batch=1/4 digest equality、取消第 2 层和关闭后 VRAM allocated/reserved=0 均通过，CPU control 通过，专项测试 6/6。WDDM overlap=0%，因此不宣称吞吐优化；未接生产 runtime。下一票为 `MF-MEM-N2R` 真实 INT8/NF4 工件对照。
- [x] MF-MEM-N2R 真实 7B NF4 placement/层迁移门：`scripts/model_fleet_n2r_artifact_gate.py` + `scripts/model_fleet_n2r_compare.py` + `fixtures/experiment-plans/plan-model-fleet-n2r-artifact-gate-v1.json` + `plan-model-fleet-n2r-real-compare-v1.json`；固定 Safetensors 15,231,233,024 bytes，artifact gate 3/3、真实对照 6/6，resident/auto context=128 batch=1 digest 一致，context=2048 batch/吞吐点通过，28 层显式 page-in/out 各 28 次，CPU-only 因 16 GB RAM 安全预算明确拒绝。未接生产 runtime。
- [x] MF-MEM-N2F 真实固定 64 MiB bridge/取消/长输出门：`scripts/model_fleet_n2r_compare.py` + `fixtures/experiment-plans/plan-model-fleet-n2f-fixed-tile-v1.json`；64 MiB × 2 bridge 计划 `3/3 passed`，ctx128 完整与 ctx128/8192 layer-boundary cancellation 通过，补充 output 32/128 完成，专项回归 9/9。桥接只限制单次传输块，模块驻留不等于 64 MiB 显存上限；未接生产 runtime。
- [x] MF-MEM-N2P 真实预取/延迟决策：`scripts/model_fleet_n2r_compare.py` + `fixtures/experiment-plans/plan-model-fleet-n2p-latency-v1.json`；resident/serial pager/distance=1 三单元 `3/3 passed`，同 digest `e958d843…`；distance=1 host staging overlap 100% 但 CUDA H2D overlap 0%，E2E 约慢 1.0%、TTFT 约慢 6.7%、inter-token P95 仅改善约 3.6%，取消回收补充门通过。结论 `Freeze` 双槽 host prefetch 默认优化，保留显式 pager 容量 profile；N3 仍需原生 Linux + 32 GB RAM。
- [ ] 剩余环境：真实 gated 账号、认证代理凭据引用、Linux Secret Service/DBus 实机门和 macOS Keychain adapter；不把本地模拟 prepare 写成跨 PC 模型已部署。

测试：M3 网络/安全、pull、artifact/runtime/deployment 相关用例均通过，并新增流中断后严格 Range、慢流不受响应头超时误杀、有限重试耗尽、跨服务实例 partial 恢复、loopback HTTP 限制和真进程 smoke；control-svc 当前全量 **274/274 passed**，生产依赖审计 0 vulnerability。公开小工件 Range、真实模型权重完整 pull、真实进程重启 partial 和代理 socket 掉线均已验收；真实 gated/认证代理与非 Windows credential adapter 仍未验收。

验收：公开仓库、gated fixture、断网续传、代理、摘要错误、空间不足、更换默认源和取消全部通过；不修改全局解释器、`HF_HOME` 或 PC/Android 既有打包依赖。

### M4：Tailscale 多集群档案

任务：

1. PC/Android/TUI 增加 cluster profile CRUD 和连接测试。
2. 主节点增加 advertised endpoint 配置和自检。
3. 调整发现优先级，不再把远端数据库作为必需路径。
4. 加入 cluster ID/epoch 防串线和主节点显示一致性测试。

**进度（2026-08-08 复核）**：

- [x] 任务 1（控制面）：`cluster-profile-repository.ts`（CRUD、`cluster_id UNIQUE` 幂等——endpoint 变更不创建重复集群、`getByCluster` 多集群隔离、markVerified）+ `cluster-profiles.controller.ts`：`POST /cluster/profiles/verify`（无副作用探测 `/bootstrap/info`，返回 cluster_id/version/master_node_id，不可达返回 unreachable 且不写状态）、`POST/GET/DELETE /cluster/profiles`、`GET /cluster/endpoints`（first-connect 登记的 endpoint 可见）、`POST /cluster/endpoints/verify`（自检 /health 可达性）。
- [x] 任务 4（防串线主体）：cluster_id 唯一约束 + 多集群并存互不串用（测试覆盖 c1/c2 并存、删除互不影响）；`key_ref` 只存引用不存明文。
- [x] 任务 3（控制面本地门）：`ClusterProfileSelectionService` 提供 activate/current，`ClusterDiscoveryService` 固定 active profile → `QLH_MASTER_HOST` → Tailscale peer 优先级并去重。
- [x] 任务 4（契约本地门）：profile API 返回 schema v1 结构化 `master_endpoint`，删除当前档案会清理选择；两个 cluster 可并存、切换互不串线。
- [x] 任务 1/2（`NET-N1` 本地双栈门）：Python/Node/Kotlin 统一 bare host 与 IPv6 URL authority；profile/endpoint verify、bootstrap manifest、advertised host 支持 IPv4、Tailscale ULA `fd7a:115c:a1e0::/48`、MagicDNS 和 `[IPv6]:port`。Python API、control-svc、gateway 和分发服务外部入口完成双栈监听；内部 inference/scheduler HTTP 继续保持同机 loopback。
- [x] 任务 2（跨平台监听门）：`scripts/ipv6_dual_stack_gate.py` 在 Windows 启动真实 Python API/control-svc/gateway，三者均接受 `127.0.0.1` 与 `::1`；Windows/WSL `socket-helper` 同端口 IPv4/IPv6 socket 与 `IPV6_V6ONLY=1` 均通过。
- [ ] 任务 1/2 剩余（客户端/实网侧）：PC/Android/TUI 档案 UI、advertised endpoint 编辑，以及 IPv4-only/IPv6-only/双栈双机矩阵；真实 Tailscale IPv6/MagicDNS 多集群重连、换网和防串线仍需真机网络。

测试：新增 `control/test/network-address.e2e-spec.ts`，并扩展 bootstrap/profile IPv6 契约；control-svc 当前全量 **301/301 passed**，gateway **104/104 passed**，Python 全量 **1729 passed / 7 skipped**，Android Full Debug Kotlin 编译通过。

验收：用户输入 IPv4、`[IPv6]:port` 或 MagicDNS 均可完成接入；远端 DB 关闭时仍能发现、注册和重连主节点；多集群切换不会串用密钥或节点 ID。自动门已满足，本条在双机纯 IPv6/混合网与真实 Tailscale IPv6 通过前保持 `Validating`。

### M5.0：无硬件部署模拟器（已完成本地门）

`control/src/data/deployment-simulator.ts` 和
`control/src/modules/models/deployment-simulation.controller.ts` 提供本地
`prepare → activate → partial → rollback` 模拟路径。输入是 artifact digest、runtime profile、节点能力矩阵、
runtime fingerprint 和 `available` 状态；错误 digest、离线节点、能力缺失、runtime 未检查/非 ready/指纹变化均 fail-closed，不进入 `actual_nodes`。
每个节点记录对齐 `schemas/deployment.schema.json`，SQLite 持久化 plan/epoch/节点摘要，重启后可继续
读取并回滚。activate 会重新读取 runtime record，prepare 后失效不能激活。`control/test/deployment-simulator.e2e-spec.ts` 已验证三节点 partial、epoch 1 激活、
epoch 2 rollback、四类 runtime 拒绝和 TOCTOU 失效门。该实现不发起网络请求，不代表真实 Worker、Tailscale 分发或性能通过。

### M5：跨节点模型部署控制器

前置：PC 完整 Worker 和物理设备准入完成。

任务：

1. scheduler-svc 实现兼容过滤、部署计划和两阶段激活。
2. 扩展现有 `model_sync.py` 协议为认证的 artifact 分发协议。
3. 实现节点预热、并发限制、断点续传、partial 状态和回滚。
4. 任务统计记录 artifact digest、计划节点、实际参与节点和 fallback。

验收：Qwen 与 DeepSeek 各至少一个 PyTorch 工件、两个 GGUF 工件在两台真实 PC 上完成下载、分发、切换、断网恢复和回滚；错模型节点不能进入流水线。

### M6：Ollama 类体验与可选兼容层

任务：

1. UI/TUI 提供 pull/list/show/remove/import 的完整命令。
2. 评估提供 Ollama 风格兼容 API，映射到 QLH job/artifact，而不是另建存储。
3. 支持模型配方：base + system prompt + template + runtime parameters；adapter 另设强校验。
4. 增加导入/导出 manifest，支持离线迁移。

验收：新用户从空模型目录到首次推理无需手工移动文件；同一操作在 UI、TUI 和 API 中状态一致。

### M7：生态扩展

候选：ModelScope、私有 S3/OCI 制品库、LoRA、embedding/reranker、多模态、Diffusers、Android 设备端直下和主节点/Worker P2P blob 交换。新增来源统一通过 §7.1 的默认源/用户更换源机制接入（源管理 + 强制重新 resolve），不绕过准入。

每个来源和模型族独立通过准入，不以 M3/M5 完成自动宣称支持。

---

## 17. 测试与发布门

### 17.1 自动化

| 类别 | 必测内容 |
|---|---|
| Schema | 版本迁移、未知字段、旧数据导入、幂等 |
| Downloader | dry-run、Range、重试、取消、revision 改变、代理、gated token |
| Artifact | 路径逃逸、摘要、原子提交、去重、引用与 GC |
| Inspector | 缺配置、缺 tokenizer、GGUF 分片、未知架构、remote code |
| Persistence | SQLite 重启恢复、远端断开、outbox 回放、重复/乱序事件 |
| Cluster profile | IPv4/IPv6/MagicDNS、错误端口、多集群、cluster ID 冲突 |
| Auth App/TOTP | Base32/`otpauth://`/二维码互通、首码确认、时间漂移、重复码拒绝、限速锁定、恢复码一次性消费、零日志和密钥封装 |
| User management | owner/admin/member 权限、账户撤销、主节点重启/备份恢复、认证域不投影远端 PG |
| Tailscale binding | 稳定 `user.id`/`tailnetId`、一一约束、pending 换网、旧授权撤销、跨 tailnet 失败回滚、loginName 变更不影响绑定 |
| Deployment | prepare/activate、partial、回滚、错 digest、Worker 离线 |
| Security | token/密钥零日志、SSRF、路径穿越、压缩炸弹、未授权分发 |

### 17.2 真实环境

- Windows 集显版：GGUF 下载、试加载、删除和回滚；更换默认源（如国内镜像）后完成一次一键 pull。
- Windows CUDA 版：Qwen/DeepSeek Safetensors 下载、量化加载、模型切换。
- Linux PC：本地数据库、权限、systemd 重启和模型目录迁移。
- Android Full：cluster profile、主节点远程下载或 SAF 导入；Lite 不误下载大模型。
- Tailscale 直连与 DERP：bootstrap、manifest、Range 分发、断线续传。
- 首次使用：无 email 的 QLH owner 创建、Auth App 字符串/二维码导入、恢复码确认；已有 Tailscale 账号授权和无账号官方注册引导各留存脱敏证据。
- PostgreSQL 故障注入：DNS 失败、拒绝连接、慢连接、运行中断开、恢复回放。

### 17.3 发布硬门

以下任一项不满足，不得宣传“一键部署”或“数据库自动接管”：

1. 下载进度和最终摘要可验证，失败不会覆盖当前模型。
2. 远端数据库不可用时，不出现启动长阻塞、内存假持久化或主节点误离线。
3. 主节点本地数据库重启恢复通过，且本地写失败会明确报错。
4. 分布式任务能证明实际参与节点和完全一致的 artifact digest。
5. 安装包不修改用户全局 Python、Hugging Face cache 或系统代理。
6. 私有 token、集群密钥和数据库密码不进入日志与导出文件。
7. Auth App/TOTP 和 Tailscale 绑定的本地主节点所有权、恢复与撤销门通过；未通过时不得宣传“账号可切换 tailnet”或“无需用户自行保管认证数据”。

---

## 18. 风险与止损

| 风险 | 对策/止损 |
|---|---|
| 试图支持任意 HF 模型导致适配无限膨胀 | 目录分级；未知模型只 inspection，不自动部署 |
| 双数据库冲突 | local-first + 单向 outbox；不支持 Navicat 直接双向写 |
| SQLite 多进程写锁 | control-svc 单写者，其他服务走 API |
| 大模型耗尽磁盘 | dry-run、配额、保留空间和引用安全 GC |
| Worker 重复下载占满出口 | 主节点缓存优先；后续评估 P2P，但以摘要为准 |
| 模型名相同内容不同 | 运行和调度只信 artifact digest |
| remote code 供应链风险 | 默认关闭；curated + revision + digest + sidecar |
| 自动主节点切换造成脑裂 | 本地投影未追平时禁止自动提升；cluster epoch fencing |
| 为兼容 Ollama 引入第二套存储 | 兼容 API 只映射现有 artifact/job，不复制 blob |
| 丢失 Auth App 与恢复码导致用户锁死 | 首次注册强制确认恢复码；主节点提供用户主动的加密备份/离线恢复；开发组不保留 reset 能力 |
| 把 Tailscale loginName/email 当永久身份 | 只绑定 API `user.id` + `tailnetId`，名称变更只更新展示字段 |
| Tailscale OAuth alpha/单 tailnet 或协议变化 | 默认不依赖；BYO app 仅实验；能力/条款门未通过就冻结 N3 |

---

## 19. 推荐实施顺序

```text
微服务清理门
   -> M0 契约
   -> M1.1 SQLite 本地事实源基线
   -> M1.2 SQLite 资产自持与备份恢复
   -> M1.3 远端 PostgreSQL 退场准备
   -> M2 工件库/导入
   -> M3 HF/GGUF 一键 pull
   -> M4 Tailscale 集群档案
   -> MF-AUTH-N1 本地认证/用户管理
   -> MF-AUTH-N2 Tailscale 一一绑定与换网
   -> M5 跨节点部署
   -> M6 Ollama 类完整体验
   -> M7 扩展来源与模型族
```

M1 必须早于真正的一键下载任务：没有本地事务状态，下载中断、进程重启和远端数据库断线都会重新产生“表面有进度、实际不可恢复”的问题。M5 必须晚于真实 PC Worker 准入：模型分发成功不能替代推理链路正确性。

---

## 20. 调研依据

- [Ollama API：pull/create/list/show/delete 与 blob 语义](https://github.com/ollama/ollama/blob/main/docs/api.md)
- [Ollama 导入 Safetensors、GGUF 与量化](https://docs.ollama.com/import)
- [Ollama Modelfile 参考](https://docs.ollama.com/modelfile)
- [Hugging Face Hub 下载与 revision/过滤/本地目录](https://huggingface.co/docs/huggingface_hub/guides/download)
- [Hugging Face Hub 缓存与完整快照](https://huggingface.co/docs/huggingface_hub/main/guides/manage-cache)
- [llama.cpp 仓库与 Hugging Face/GGUF 运行入口](https://github.com/ggml-org/llama.cpp)
- [GGUF 格式说明](https://github.com/ggml-org/ggml/blob/master/docs/gguf.md)
- [Tailscale 地址稳定性](https://tailscale.com/kb/1033/ip-and-dns-addresses/)
- [Tailscale MagicDNS](https://tailscale.com/docs/features/magicdns)
- [Tailscale grants/ACL policy](https://tailscale.com/kb/1337/policy-syntax)
- [Tailscale 支持的身份提供方与“不是身份提供方”说明](https://tailscale.com/docs/integrations/identity)
- [Tailscale OAuth clients（client credentials/API scopes）](https://tailscale.com/docs/features/oauth-clients)
- [Tailscale OAuth Apps 设备授权（当前 alpha、单 tailnet）](https://tailscale.com/docs/features/oauth-apps/device-provisioning)
- [Tailscale API（用户稳定 ID、tailnet ID 与用户角色）](https://tailscale.com/api)
- [Tailscale Quickstart（官方注册与客户端登录流程）](https://tailscale.com/docs/how-to/quickstart)
- [RFC 6238：TOTP 算法与密钥/时间步安全要求](https://www.rfc-editor.org/rfc/rfc6238)
- [Google Authenticator Key URI Format：`otpauth://` 字符串与二维码载荷](https://github.com/google/google-authenticator/wiki/Key-Uri-Format)
- [Aegis Authenticator 仓库（HOTP/TOTP、二维码/手工录入、GPL-3.0）](https://github.com/beemdevelopment/Aegis)
- [SQLite WAL](https://www.sqlite.org/wal.html)
- [SQLite Backup API](https://www.sqlite.org/backup.html)

---

## 21. 本计划完成定义

只有同时满足以下条件，`L4-MODEL-FLEET` 才能由 Candidate 转为 Completed：

1. 空安装用户使用开发团队默认源即可完成解析、下载、校验、试加载和首次推理；也可更换自定义源后完成同样流程。
2. 用户可以登记 Tailscale/MagicDNS 主节点，PC 与 Android 重启后自动恢复正确 cluster profile。
3. 干净安装在没有 PostgreSQL、远端账号和开发组服务时仍可启动、认证、导入、部署、重启恢复和本地推理；远端 PostgreSQL 若存在，只能作为用户主动开启的兼容导出出口。
4. 两台真实 PC 完成至少一个 PyTorch 与一个 GGUF 工件的分发、激活、实际推理和回滚。
5. UI/TUI/API 的模型状态一致，且任务统计只显示真实参与节点。
6. 供应链、密钥、许可证、磁盘和故障注入发布门全部通过。
7. 认证域在用户自己的主节点闭环：Auth App 字符串/二维码注册、TOTP 登录、恢复码、限速/撤销和加密备份通过；认证数据不进入开发组服务或远端 PostgreSQL。
8. QLH 用户与 `tailnet_id + tailscale_user_id` 一一绑定，已有 Tailscale 账号和首次注册引导均可完成；换 tailnet 为 pending/confirm/revoke 原子流程，旧绑定不能继续访问。
9. 任一集中 Tailscale OAuth、GitHub OAuth 或 Aegis 衍生再分发方案都先通过条款/许可证/合作方评审；未通过时默认路径仍不依赖它们。
10. 用户可以在自己的设备/NAS/对象存储上完成加密备份和新主节点恢复，模型工件、配置、任务、会话和认证数据不依赖开发组或远端数据库；第三方模型许可证仍按来源条款执行。
